---
phase: 01-environment-oracle-foundation
plan: 4
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - Makefile
  - docs/environment.md
  - README.md
autonomous: true
requirements: [INFRA-02, INFRA-01, INFRA-03]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "make up / make down / make reset / make logs work as documented; make down preserves named volumes, make reset removes them (docker compose down -v)."
    - "docs/environment.md documents CPU/RAM/disk requirements based on an actual docker stats / docker system df observation of the full running stack — not just summed vendor minimums."
    - "A fresh make reset && make up from a clean state brings all 5 Oracle tables and every Airflow service to verified-healthy state, confirmed by scripts/verify_environment.py and an Airflow admin/admin auth-token check."
  artifacts:
    - path: "Makefile"
      provides: "up/down/reset/logs targets (D-14/D-15)"
    - path: "docs/environment.md"
      provides: "Resource requirements + WSL2/.wslconfig + networking guidance (D-17)"
    - path: "README.md"
      provides: "Minimal clone-to-running entry point linking to docs/environment.md"
  key_links:
    - from: "Makefile"
      to: "docker-compose.yml"
      via: "docker compose up -d --wait / down / down -v / logs -f targets"
      pattern: "docker compose"
    - from: "README.md"
      to: "docs/environment.md"
      via: "markdown link from the getting-started section"
      pattern: "docs/environment.md"
  prohibitions:
    - "MUST NOT publish CPU/RAM/disk numbers in docs/environment.md that were never empirically measured against the actual running stack in this phase's own execution — no copy-pasted vendor minimums presented as this project's own measurement"
---

<objective>
Establish the Makefile as the project-wide command entrypoint (D-14, not just this phase's scope),
document real, empirically-observed CPU/RAM/disk requirements (INFRA-02) alongside WSL2/Docker
Desktop networking guidance, and run the phase-gate verification that ties together every success
criterion from ROADMAP.md Phase 1 against a genuinely fresh `make reset && make up`.

Purpose: RESEARCH.md's Open Question 1 flags that Airflow's own documented floor (4GB RAM/2 CPU/
10GB disk) plus Oracle Free's separate footprint is only a starting estimate — INFRA-02 explicitly
requires the documented number to match real-world practice, which this plan closes by actually
running `docker stats`/`docker system df` against the finished stack rather than summing vendor
minimums.

Output: Makefile, docs/environment.md, and a minimal README.md exist; the full phase is verified
end-to-end from a clean state.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-environment-oracle-foundation/01-CONTEXT.md
@.planning/phases/01-environment-oracle-foundation/01-RESEARCH.md
@.planning/phases/01-environment-oracle-foundation/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Makefile — up/down/reset/logs (D-14/D-15)</name>
  <files>Makefile</files>
  <read_first>
    .planning/phases/01-environment-oracle-foundation/01-RESEARCH.md (Makefile skeleton in Code Examples)
    .planning/phases/01-environment-oracle-foundation/01-PATTERNS.md (Makefile section — .PHONY + `## description` self-documentation convention)
  </read_first>
  <action>
    Create `Makefile` at repo root with `.PHONY: up down reset logs` declared up front. `up:`
    target runs `docker compose up -d --wait`, comment `## Start the full stack (Airflow + Oracle)`.
    `down:` target runs `docker compose down`, comment `## Stop containers only — named volumes
    (D-13) stay intact` (never `-v` — D-15 requires `make down` to never silently destroy data).
    `reset:` target runs `docker compose down -v`, comment `## Full wipe: stop containers AND
    remove volumes (D-15)`. `logs:` target runs `docker compose logs -f`, comment `## Tail logs
    from every service`. Every target has a trailing `## description` comment (D-14's
    self-documentation convention, matched from the reference repo's structural pattern per
    PATTERNS.md — target bodies themselves are this project's own, not copied). Use literal tab
    characters before each recipe line, not spaces (Make requires this). Later phases (2-6) add
    targets here (`make test`, `make lint`, `make verify`, `make benchmark`) rather than inventing
    separate tooling — do not scope this Makefile as "Phase 1 only."
  </action>
  <verify>
    <automated>
      make -n up | grep -c "docker compose up"
      make -n down | grep -c "docker compose down$"
      make -n reset | grep -c "docker compose down -v"
    </automated>
  </verify>
  <acceptance_criteria>
    - `make -n up` shows `docker compose up -d --wait` (dry-run, no execution)
    - `make -n down` shows exactly `docker compose down` with no `-v` flag
    - `make -n reset` shows `docker compose down -v`
    - `grep -c '^\t' Makefile` is greater than 0 (confirms literal tabs, not spaces, before recipes)
  </acceptance_criteria>
  <done>Makefile exists with correct up/down/reset/logs targets, down never destroys volumes.</done>
</task>

<task type="auto">
  <name>Task 2: Observe real resource usage + write docs/environment.md (INFRA-02, D-17)</name>
  <files>docs/environment.md</files>
  <read_first>
    .planning/research/PITFALLS.md (`.wslconfig` sizing and IPv4/mirrored-networking guidance — carry forward verbatim)
    .planning/phases/01-environment-oracle-foundation/01-RESEARCH.md (Common Pitfall #5 — Airflow's own literal preflight thresholds; Open Question 1)
  </read_first>
  <action>
    With the full stack running (`make up` from Task 1, using Plan 01-03's custom image), run
    `docker stats --no-stream --format "table {{.Name}}\t{{.CPUPerc}}\t{{.MemUsage}}"` and `docker
    system df` and capture the actual output. Write `docs/environment.md` documenting: (1)
    Airflow's own literal preflight floor — 4000 MB RAM, 2 CPUs, 10 GB disk (RESEARCH.md Common
    Pitfall #5, cited verbatim from the official compose's `airflow-init` preflight script) as the
    Airflow-side baseline; (2) Oracle Database Free's own separately-documented footprint on top
    (~2 GB+ RAM); (3) the actual observed peak CPU/RAM/disk from this task's own `docker
    stats`/`docker system df` run, labeled explicitly as "measured against this project's own
    docker-compose.yml on [date]" — not presented as a vendor number; (4) `.wslconfig` sizing
    guidance (memory/processors caps) carried forward from `.planning/research/PITFALLS.md`; (5)
    the IPv4/mirrored-networking caveat from the same source; (6) a note that Oracle's port 1521
    and Airflow's port 8080 are bound to `127.0.0.1` only (T-01-02) — with a one-line note on how
    to reach them from Windows-side tools via the WSL2 interface IP if cross-host access is ever
    needed, rather than widening the bind. Do not present the vendor-minimum sum as the final
    number without the empirically observed figure alongside it.
  </action>
  <verify>
    <automated>
      docker stats --no-stream
      docker system df
      test -f docs/environment.md
      grep -c "docker stats" docs/environment.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `docs/environment.md` exists and contains both the vendor-floor numbers AND a labeled
      "observed" figure from this task's own `docker stats`/`docker system df` run
    - The observed figure is not identical to the raw summed vendor minimums presented without
      attribution (the doc must distinguish "documented floor" from "what we actually measured")
    - `.wslconfig` guidance and the 127.0.0.1-only port-binding note are both present
  </acceptance_criteria>
  <done>docs/environment.md documents real, observed resource requirements — not fabricated numbers.</done>
</task>

<task type="auto">
  <name>Task 3: README entry point + full fresh-clone phase-gate verification</name>
  <files>README.md</files>
  <read_first>
    .planning/ROADMAP.md (Phase 1 Success Criteria — the full checklist this task closes)
    docs/environment.md (from Task 2 — what README links to)
  </read_first>
  <action>
    Create a minimal `README.md` with a "Getting Started" section: `git clone`, `cp .env.example
    .env` (D-09's documented first-clone step), `make up`, then a link to `docs/environment.md`
    for resource requirements and networking notes. This is intentionally minimal — the full
    clone-to-ingest documentation completeness gate is DOC-01 (Phase 6 scope); this section only
    needs to cover what Phase 1 itself delivers (the environment, not yet the DAG/ingestion flow).

    Run the full phase-gate verification: `make reset && make up` from a genuinely clean state,
    wait for all services healthy, run `uv run python scripts/verify_environment.py` (confirms all
    5 tables + columns + admin/admin Oracle auth), run the `curl -X POST
    http://localhost:8080/auth/token` check (confirms admin/admin Airflow auth), run `docker
    compose ps --format json` and confirm every service reports healthy or running, and confirm
    Oracle's port binding via `docker compose port oracle 1521` (or `ss -tlnp | grep 1521` inside
    the container) shows `127.0.0.1`, not `0.0.0.0`.
  </action>
  <verify>
    <automated>
      make reset
      make up
      uv run python scripts/verify_environment.py
      curl -s -X POST http://localhost:8080/auth/token -H "Content-Type: application/json" -d '{"username":"admin","password":"admin"}' | grep -c access_token
      docker compose ps --format json | node -e "let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>{const lines=d.trim().split('\n').filter(Boolean);const bad=lines.map(l=>JSON.parse(l)).filter(s=>s.Health && s.Health!=='healthy' && s.State!=='running');console.log(bad.length===0?'ALL_HEALTHY':'UNHEALTHY:'+JSON.stringify(bad));})"
    </automated>
  </verify>
  <acceptance_criteria>
    - `make reset && make up` succeeds from a clean state (proves the whole phase is reproducible
      from scratch, not just incrementally working)
    - `scripts/verify_environment.py` exits 0
    - Airflow auth-token curl check returns `access_token`
    - `docker compose ps` shows every service healthy/running — output is `ALL_HEALTHY`
    - README.md exists and links to docs/environment.md
  </acceptance_criteria>
  <done>All 4 ROADMAP.md Phase 1 Success Criteria are independently demonstrated true from a fresh clean-state boot.</done>
</task>

</tasks>

<flagged_assumptions>
## Flagged Assumptions (Unresolved Edge-Probe Items)

- **INFRA-02 (category: unclassified — the deterministic probe could not auto-classify this
  infra-shaped requirement).** The documented CPU/RAM/disk numbers in docs/environment.md are a
  snapshot from one `docker stats`/`docker system df` observation taken during this task's own
  execution. How the footprint behaves over a long-running development session, under host
  resource contention from other running applications, or as Oracle's partitioned tables
  accumulate real data over time is not resolved by this single snapshot. No resolution is
  fabricated here; this is flagged for developer awareness, and the doc should be revisited if a
  developer observes materially different numbers in practice.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| docs/environment.md -> developer trust | A developer sizing their `.wslconfig`/host resources relies on these documented numbers being real |
| Makefile `reset` target -> named volumes | `docker compose down -v` is destructive; must be clearly distinguished from `down` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Denial of Service (data loss) | Makefile `reset` target (`docker compose down -v`) | medium | mitigate | `make down` explicitly never removes volumes (D-15, Task 1); `reset` is its own separately-documented, separately-named target so a developer cannot accidentally destroy data via `make down` |
| T-01-02 (final confirmation) | Elevation of Privilege (network-adjacent) | Oracle/Airflow port bindings, phase-gate re-verification | medium | mitigate | Task 3's phase-gate check explicitly re-confirms `127.0.0.1`-only binding via `docker compose port`/`ss` before the phase is considered complete |
</threat_model>

<verification>
`make reset && make up` succeeds from a clean state. `scripts/verify_environment.py` exits 0.
Airflow auth-token check returns a JWT. `docker compose ps` shows all services healthy. Port
bindings confirmed loopback-only. README.md and docs/environment.md both exist.
</verification>

<success_criteria>
All 4 ROADMAP.md Phase 1 Success Criteria are independently demonstrated true from a fresh,
`make reset`-clean boot: full stack healthy, all 5 Oracle tables verified via metadata views, real
observed resource numbers documented, single admin/admin credential pair working everywhere.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

- `docker-compose.yml` — full service topology (Plans 01-01, 01-03)
- `docker/airflow/Dockerfile` — custom Airflow image (Plan 01-03)
- `docker/oracle/init/*.sql` — schema DDL (Plans 01-01, 01-02)
- `scripts/verify_environment.py` — D-05 verification script (Plans 01-01, 01-02)
- `Makefile` — lifecycle commands (this plan)
- `docs/environment.md` — resource + networking docs (this plan)
- `README.md` — minimal clone-to-running entry point (this plan)
- `.env.example` — credential template (Plans 01-01, 01-03)
- `packages/csv-processor/src/csv_processor/` — empty package scaffold (Plan 01-03)
- `airflow/dags/` — empty scaffold dir (Plan 01-03)
- `pyproject.toml` / `uv.lock` — Python project scaffolding (Plan 01-01)
</artifacts_produced>

<output>
Create `.planning/phases/01-environment-oracle-foundation/01-04-SUMMARY.md` when done
</output>
