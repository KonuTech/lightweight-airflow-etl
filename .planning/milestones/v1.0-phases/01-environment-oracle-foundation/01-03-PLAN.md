---
phase: 01-environment-oracle-foundation
plan: 3
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - docker/airflow/Dockerfile
  - docker-compose.yml
  - .env.example
  - packages/csv-processor/pyproject.toml
  - packages/csv-processor/src/csv_processor/__init__.py
  - airflow/dags/.gitkeep
autonomous: true
requirements: [INFRA-01, INFRA-03]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "docker/airflow/Dockerfile builds successfully FROM apache/airflow:3.3.1-python3.12, installing oracledb==4.0.2, pydantic==2.13.4, apache-airflow-providers-standard==1.18.0 (pinned + constrained), and the local csv_processor package scaffold with --no-deps."
    - "docker-compose.yml's four Airflow runtime services build from the custom Dockerfile rather than the stock base image, and the stack still boots healthy."
    - "An Airflow Connection oracle_default (registered via AIRFLOW_CONN_ORACLE_DEFAULT, D-11) is visible/testable in Airflow, independent of csv_processor, which never touches Connection objects (ENGINE-09)."
  artifacts:
    - path: "docker/airflow/Dockerfile"
      provides: "Custom Airflow worker image per D-12"
    - path: "packages/csv-processor/src/csv_processor/__init__.py"
      provides: "Empty csv_processor package scaffold — no processing logic yet (Phase 3 scope)"
    - path: "airflow/dags/.gitkeep"
      provides: "Empty DAG scaffold dir — no DAG code yet (Phase 5 scope)"
  key_links:
    - from: "docker-compose.yml"
      to: "docker/airflow/Dockerfile"
      via: "build.dockerfile directive on all 4 Airflow runtime services"
      pattern: "docker/airflow/Dockerfile"
    - from: ".env.example"
      to: "Airflow's Connection registry"
      via: "AIRFLOW_CONN_ORACLE_DEFAULT env var, auto-registered by Airflow at boot (no CLI/init step)"
      pattern: "AIRFLOW_CONN_ORACLE_DEFAULT"
  prohibitions: []
---

<objective>
Replace the stock `apache/airflow:3.3.1-python3.12` image used by Plan 01-01's tracer with a
custom-built image (D-12) that pip-installs this project's pinned dependencies and the local
`csv_processor` package scaffold, and register an Airflow Connection for Oracle (D-11) purely for
UI visibility — `csv_processor` itself never touches Connection objects and stays Airflow-agnostic
(ENGINE-09). Also lay down the empty `packages/csv-processor/` and `airflow/dags/` scaffold
directories that Phases 2-5 will fill in, per D-16's locked repo layout.

Purpose: D-12 explicitly rejects `_PIP_ADDITIONAL_REQUIREMENTS` (Airflow's own docs call it
dev-only, no lockfile) in favor of a pinned, constrained, custom-built image — this must land now,
not be deferred, because every later phase's Airflow container needs `oracledb`/`pydantic`/
`csv_processor` already installed.

Output: docker-compose.yml's Airflow services build from the custom image; `oracle_default`
Connection is registered and testable; empty package/DAG scaffolds exist for later phases to fill.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-environment-oracle-foundation/01-CONTEXT.md
@.planning/phases/01-environment-oracle-foundation/01-RESEARCH.md
@.planning/phases/01-environment-oracle-foundation/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Custom Airflow Dockerfile + docker-compose build wiring + csv-processor/dags scaffolds</name>
  <files>docker/airflow/Dockerfile, docker-compose.yml, .env.example, packages/csv-processor/pyproject.toml, packages/csv-processor/src/csv_processor/__init__.py, airflow/dags/.gitkeep</files>
  <read_first>
    .planning/phases/01-environment-oracle-foundation/01-RESEARCH.md (Pattern 1, Pattern 5, Code Examples — exact Dockerfile and Connection env var syntax)
    .planning/phases/01-environment-oracle-foundation/01-PATTERNS.md (docker/airflow/Dockerfile section — full worked adaptation from the reference repo)
    docker-compose.yml (from Plan 01-01 — the x-airflow-common anchor and 4 Airflow service blocks to edit)
  </read_first>
  <action>
    Create `docker/airflow/Dockerfile`: `FROM apache/airflow:3.3.1-python3.12` — no `USER root`
    switch anywhere (the base image already runs as the non-root `airflow` user, uid 50000/gid 0).
    `RUN pip install --no-cache-dir "oracledb==4.0.2" "pydantic==2.13.4"
    "apache-airflow-providers-standard==1.18.0" --constraint
    "https://raw.githubusercontent.com/apache/airflow/constraints-3.3.1/constraints-3.12.txt"`
    (packages already reviewed for legitimacy in Plan 01-01's Task 2 checkpoint — proceed without
    re-asking). Then `COPY --chown=airflow:0 packages/csv-processor/pyproject.toml
    packages/csv-processor/pyproject.toml`, `COPY --chown=airflow:0 packages/csv-processor/src
    packages/csv-processor/src`, `RUN pip install --no-cache-dir --no-deps packages/csv-processor/`
    (always `--chown=airflow:0` on every COPY, always `--no-deps` on the local package install —
    RESEARCH.md Pattern 1).

    Create `packages/csv-processor/pyproject.toml` — minimal valid package metadata (name
    `csv-processor`, version `0.1.0`, requires-python `>=3.12`, no dependencies listed yet — Phase
    3 adds them). Create `packages/csv-processor/src/csv_processor/__init__.py` — empty file (or a
    single `__version__ = "0.1.0"` line), no processing logic — Phase 3's scope, not this phase's.
    Create `airflow/dags/.gitkeep` — empty file, no DAG code — Phase 5's scope.

    Edit `docker-compose.yml`: on all 4 Airflow runtime services (`airflow-init`,
    `airflow-apiserver`, `airflow-scheduler`, `airflow-dag-processor`, `airflow-triggerer` — note
    this is 5 services total including airflow-init), replace `image:
    apache/airflow:3.3.1-python3.12` with a `build:` block: `context: .`, `dockerfile:
    docker/airflow/Dockerfile`. Add `AIRFLOW_CONN_ORACLE_DEFAULT` to the shared
    `x-airflow-common` environment block, value
    `'oracle://admin:admin@oracle:1521/?service_name=FREEPDB1&encoding=UTF-8&threaded=False&events=False'`
    (D-11, RESEARCH.md Pattern 5 — Airflow auto-registers any `AIRFLOW_CONN_*` env var as a
    Connection, no CLI/init step needed). Note the hostname is `oracle` (the compose service name,
    Docker's internal DNS), not `localhost` — this is a container-to-container connection, unlike
    the host-side `scripts/verify_environment.py`.

    Edit `.env.example`: append a comment noting `AIRFLOW_CONN_ORACLE_DEFAULT` is set directly in
    docker-compose.yml (not sourced from `.env`) because it embeds the container-internal hostname
    `oracle`, which is not a developer-facing secret value.
  </action>
  <verify>
    <automated>
      docker compose build
      docker compose up -d --wait
    </automated>
  </verify>
  <acceptance_criteria>
    - `docker compose build` succeeds with no errors for all Airflow services
    - `docker compose up -d --wait` brings the full stack healthy using the custom-built image
    - `docker compose exec airflow-scheduler python -c "import csv_processor; import oracledb;
      import pydantic"` exits 0 (confirms all 3 pinned packages plus the local scaffold package
      import cleanly inside the built image)
  </acceptance_criteria>
  <done>Custom Airflow image builds and boots; csv_processor and airflow/dags scaffolds exist.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Oracle Connection registration and re-confirm environment integrity</name>
  <files>docker-compose.yml</files>
  <read_first>
    .planning/phases/01-environment-oracle-foundation/01-RESEARCH.md (Open Question 2 — DSN shape uncertainty this task resolves empirically)
  </read_first>
  <action>
    With the stack from Task 1 running, confirm the `oracle_default` Connection Airflow
    auto-registered from `AIRFLOW_CONN_ORACLE_DEFAULT` is usable: run `docker compose exec
    airflow-scheduler airflow connections get oracle_default` and confirm it resolves without
    error, then `docker compose exec airflow-scheduler airflow connections test oracle_default`
    and confirm it reports a successful connection test (this resolves RESEARCH.md's Open Question
    2 — whether the `?service_name=FREEPDB1` URI form parses correctly — empirically, rather than
    assuming the docs snippet is correct). Re-run `uv run python scripts/verify_environment.py`
    from the host to confirm switching the Airflow services to the custom-built image did not
    regress Plan 01-01/01-02's Oracle table or Airflow auth checks.
  </action>
  <verify>
    <automated>
      docker compose exec airflow-scheduler airflow connections get oracle_default
      docker compose exec airflow-scheduler airflow connections test oracle_default
      uv run python scripts/verify_environment.py
    </automated>
  </verify>
  <acceptance_criteria>
    - `airflow connections get oracle_default` exits 0 and prints the connection details
    - `airflow connections test oracle_default` reports success (not a parse or auth error)
    - `uv run python scripts/verify_environment.py` still exits 0 (no regression from the image swap)
  </acceptance_criteria>
  <done>oracle_default Connection is registered and verified working; no regression in prior verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| PyPI -> Docker image build layer | Supply-chain: `oracledb`, `pydantic`, `apache-airflow-providers-standard` installed from PyPI into the built Airflow image |
| Airflow API server -> Oracle (Connection object) | `AIRFLOW_CONN_ORACLE_DEFAULT` carries the admin/admin credential as a container-internal connection URI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | `docker/airflow/Dockerfile`'s `pip install` layer | high | mitigate | Packages already reviewed via Plan 01-01's Task 2 package-legitimacy checkpoint (`oracledb`, `pydantic`, `apache-airflow-providers-standard`); Dockerfile pins exact versions plus a constraints URL, no floating versions |
| T-01-05 | Information Disclosure | `AIRFLOW_CONN_ORACLE_DEFAULT` stored as a plaintext env var in docker-compose.yml | low | accept | Local-dev-only throwaway value, consistent with this project's explicit no-Vault scope decision (project-level); the credential itself is already gitignored at its source (`.env`, T-01-01 in Plan 01-01) |
</threat_model>

<verification>
`docker compose build && docker compose up -d --wait` succeeds using the custom image.
`oracle_default` Connection registers and tests successfully. `scripts/verify_environment.py`
still passes — no regression.
</verification>

<success_criteria>
The Airflow services run from a pinned, constrained, locally-built image containing this project's
dependencies and the (still-empty) csv_processor package; the Oracle Connection is registered for
UI visibility per D-11 without csv_processor depending on it.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

- `docker-compose.yml` — full service topology (Plan 01-01 creates it; this plan edits it)
- `docker/airflow/Dockerfile` — custom Airflow image (this plan)
- `docker/oracle/init/*.sql` — schema DDL (Plans 01-01, 01-02)
- `scripts/verify_environment.py` — D-05 verification script (Plans 01-01, 01-02)
- `Makefile` — lifecycle commands (Plan 01-04)
- `docs/environment.md` — resource + networking docs (Plan 01-04)
- `.env.example` — credential template (Plan 01-01 creates it; this plan extends it)
- `packages/csv-processor/src/csv_processor/` — empty package scaffold, no logic yet (this plan)
- `airflow/dags/` — empty scaffold dir, no DAG code yet (this plan)
</artifacts_produced>

<output>
Create `.planning/phases/01-environment-oracle-foundation/01-03-SUMMARY.md` when done
</output>
