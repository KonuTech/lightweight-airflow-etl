---
phase: 02-config-contract-csv-generator
plan: 05
type: execute
wave: 4
depends_on: ["02-04"]
files_modified:
  - tests/fixtures/corpus.yaml
  - tests/fixtures/CORPUS.sha256
  - tools/corpus/generators.py
  - tests/unit/test_corpus_bounded_memory.py
  - Makefile
autonomous: true
requirements: [GEN-01]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The gzip-wrapped fixture (29_gzip_wrapped_valid_file) is written via gzip.GzipFile(mtime=0, filename=''); the zip-wrapped fixture (30_zip_wrapped_valid_file) via zipfile.ZipInfo(date_time=(1980,1,1,0,0,0)) — regenerating either produces byte-identical output to the committed digest oracle (R5)."
    - "The large fixture (28_large_streaming_profile) is written via batched/streamed writes, never fully materialized as a single in-memory buffer during generation, and its declared approx_bytes is more than double the RLIMIT_AS bounded-memory test's configured memory limit."
    - "A streaming (line-by-line) read of the large fixture under a subprocess RLIMIT_AS memory cap completes successfully (returncode 0); this is proven independently of, and in addition to, Phase 6's own ~100K-row empirical benchmark (TEST-04)."
    - "make verify-phase2 (running the full unit test suite plus make fixtures-verify) is green — the phase's own local combined verification gate (D-16g)."
  prohibitions:
    - statement: "The RLIMIT_AS bounded-memory subprocess test MUST NOT silently skip and report a passing result on a platform where the `resource` module / RLIMIT_AS is unavailable (e.g. non-POSIX) — an unsupported platform must produce a visible, explicit pytest skip with a stated reason, never a silent pass that would misrepresent the memory-boundedness proof as having run."
      category: transparency
      status: resolved
      verification: test
  artifacts:
    - tests/fixtures/corpus.yaml (extended to ~30 fixtures)
    - tests/fixtures/CORPUS.sha256 (final)
    - tools/corpus/generators.py (wrapper kind added)
    - tests/unit/test_corpus_bounded_memory.py
  key_links:
    - "tests/fixtures/csv/28_large_streaming_profile.csv -> tests/unit/test_corpus_bounded_memory.py's subprocess -> resource.setrlimit(RLIMIT_AS, ...)"
    - "make verify-phase2 -> uv run pytest tests/unit/ -x && make fixtures-verify (D-16g, the phase's own combined local gate)"
---

<objective>
Complete the fixture corpus with the large/compressed category (3 fixtures: one oversized
streaming-generated file, one gzip-wrapped fixture, one zip-wrapped fixture — D-16b/D-16c), add
the `RLIMIT_AS`-bounded subprocess memory test the reference repo's own ADR describes but never
implemented (new authorship, per RESEARCH.md's Assumption A4), and close out the phase with its
own local combined verification gate (`make verify-phase2`, D-16g).

Purpose: prove the corpus's `wrapper` generator kind (gzip/zip determinism, R5) and give Phase 3
an oversized fixture with an independent, non-benchmark proof that a streaming reader stays under
a hard memory cap — then prove the entire phase (config, generator, corpus) is green in one
command before `/gsd-verify-work` runs.
Output: the corpus's final ~30th fixture committed; `tests/unit/test_corpus_bounded_memory.py`
passing; `make verify-phase2` green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-config-contract-csv-generator/02-CONTEXT.md
@.planning/phases/02-config-contract-csv-generator/02-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Large/compressed fixture category (3 fixtures) — `wrapper` generator kind</name>
  <files>tests/fixtures/corpus.yaml, tests/fixtures/CORPUS.sha256, tools/corpus/generators.py</files>
  <read_first>
    tools/corpus/generators.py (Plan 03's `stream_for`/`tabular`/`literal` kinds — the `wrapper`
    kind added here follows the same dispatch pattern),
    .planning/phases/02-config-contract-csv-generator/02-RESEARCH.md (sections "RNG derivation and
    gzip/zip determinism", "RLIMIT_AS bounded-memory technique", row "Large/compressed" in
    "Recommended scoped fixture category list"),
    /home/user/projects/airflow-platform/tools/corpus/generators.py (lines 406-435 — the reference
    repo's actual gzip `mtime=0, filename=""` and zip `ZipInfo(date_time=(1980,1,1,0,0,0))` code,
    read this phase's own RESEARCH.md already quotes it in full)
  </read_first>
  <behavior>
    - Test: generating `28_large_streaming_profile` twice produces byte-identical files (same
      per-fixture RNG determinism as every other fixture, R1), and peak resident memory during
      generation (measurable via `resource.getrusage(resource.RUSAGE_SELF).ru_maxrss` before/after,
      or by construction — batched writes, never a single joined buffer) stays well under the
      fixture's own `approx_bytes`.
    - Test: generating `29_gzip_wrapped_valid_file` twice produces byte-identical gzip output
      (proves `mtime=0, filename=""` eliminates the wall-clock/filename variance gzip headers
      normally embed).
    - Test: generating `30_zip_wrapped_valid_file` twice produces byte-identical zip output (proves
      `ZipInfo(date_time=(1980,1,1,0,0,0))` eliminates zip's own wall-clock timestamp variance).
  </behavior>
  <action>
    Implement the `"wrapper"` `GeneratorKind` in `tools/corpus/generators.py`: given a fixture
    declaring `generator: {kind: wrapper, format: gzip|zip, inner: <a small inline tabular/literal
    payload spec>}`, generate the inner payload's bytes first (reusing the existing dispatcher),
    then wrap: for `format: gzip`, write via `gzip.GzipFile(fileobj=raw, mode="wb",
    compresslevel=9, mtime=0, filename="")` (R5 — both `mtime=0` and `filename=""` are
    non-negotiable, both are embedded in the gzip header and vary run-to-run otherwise); for
    `format: zip`, write via `zipfile.ZipFile(raw, mode="w")` with a `zipfile.ZipInfo(filename=...,
    date_time=(1980, 1, 1, 0, 0, 0))` member (R5 — `ZipInfo.date_time` defaults to wall-clock time
    otherwise).

    Author 3 fixtures into `tests/fixtures/corpus.yaml` under category `large_compressed`:
    `28_large_streaming_profile` (tabular, `profile: large`, using the existing `repeat` row-spec
    kind to reach an `approx_bytes` of approximately 62,914,560 bytes / 60 MiB — generated via
    batched/flushed writes in `generators.py`'s `tabular` dispatch path, never materializing the
    whole file in one in-memory buffer; `expect`: "streaming reader stays bounded; oversized for
    Task 2's RLIMIT_AS test at a 24 MiB (25,165,824-byte) limit — approx_bytes exceeds 2x the limit,
    mirroring the reference repo's own validated `approx_bytes > 2 * rlimit_as_bytes` rule");
    `29_gzip_wrapped_valid_file` (wrapper, format `gzip`, wrapping a small ~20-row valid tabular
    payload defined inline in this fixture's own generator config); `30_zip_wrapped_valid_file`
    (wrapper, format `zip`, wrapping the same shape of small valid payload). Run `make fixtures &&
    make fixtures-verify`.
  </action>
  <verify>
    <automated>make fixtures && make fixtures-verify</automated>
  </verify>
  <acceptance_criteria>
    - `make fixtures-verify` exits 0.
    - The manifest now declares 30 total fixtures; `wc -l tests/fixtures/CORPUS.sha256` reports 30 lines.
    - `ls -la tests/fixtures/csv/28_large_streaming_profile.csv` reports a file size greater than 50,000,000 bytes.
    - `grep -n "mtime=0" tools/corpus/generators.py` and `grep -n "date_time=(1980" tools/corpus/generators.py` both return a match.
  </acceptance_criteria>
  <done>The corpus's final 3 fixtures (large streaming file, gzip wrapper, zip wrapper) are committed with byte-identical regeneration proven twice in a row; the corpus reaches its full ~30-fixture scope (D-16a).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RLIMIT_AS bounded-memory subprocess test (D-16b)</name>
  <files>tests/unit/test_corpus_bounded_memory.py</files>
  <read_first>
    .planning/phases/02-config-contract-csv-generator/02-RESEARCH.md (section "RLIMIT_AS
    bounded-memory technique" — the full code sketch is new authorship, no reference-repo
    implementation exists to port),
    tests/fixtures/csv/28_large_streaming_profile.csv (Task 1's output — the fixture this test
    exercises)
  </read_first>
  <behavior>
    - Test: on a POSIX platform, a child process that reads `28_large_streaming_profile.csv`
      streaming (one line materialized at a time via a plain `for line in handle:` loop) under a
      `resource.setrlimit(RLIMIT_AS, (25165824, 25165824))` (24 MiB) cap exits with returncode 0.
    - Test: on a POSIX platform, a deliberately-broken "buffering" variant of the same child
      process (`handle.readlines()` — loads the whole file into a list before iterating) exits
      with a non-zero returncode (typically 137/SIGKILL or a `MemoryError`-carrying failure) under
      the identical 24 MiB cap — this is the fixture's own negative control, proving the RLIMIT_AS
      cap is actually doing something rather than being vacuous.
    - Test: on a non-POSIX platform (`resource` import fails or `RLIMIT_AS` is unavailable), the
      test calls `pytest.skip("RLIMIT_AS bounded-memory test requires a POSIX resource module")`
      with an explicit, visible reason — it never silently reports a pass (this is the prohibition
      backing evidence: see `must_haves.prohibitions` above).
  </behavior>
  <action>
    Create `tests/unit/test_corpus_bounded_memory.py` (new authorship — the reference repo's ADR
    describes the `resource.setrlimit(RLIMIT_AS, ...)` technique but has no committed
    implementation to port, per RESEARCH.md's Assumption A4). At module import time, attempt
    `import resource`; if unavailable, mark every test in this file with
    `pytest.mark.skip(reason="RLIMIT_AS bounded-memory test requires a POSIX resource module")` —
    never let the test silently pass by, e.g., returning early with no assertion. Implement a
    helper that spawns `sys.executable -c "<inline script>"` (or a small standalone script file
    under `tests/unit/`) which calls `resource.setrlimit(resource.RLIMIT_AS, (limit_bytes,
    limit_bytes))` then either streams the fixture line-by-line (expected to survive) or reads it
    wholesale via `.readlines()` (expected to die under the cap) — pass `limit_bytes=25165824` (24
    MiB) and the fixture path via `subprocess.run([...], capture_output=True)`; assert on
    `result.returncode` per `<behavior>` above.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_corpus_bounded_memory.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_corpus_bounded_memory.py -x` passes (or explicitly, visibly skips with a stated reason on a non-POSIX runner — never a silent pass).
    - `grep -n "pytest.skip" tests/unit/test_corpus_bounded_memory.py` returns a match with a non-empty reason string.
  </acceptance_criteria>
  <done>D-16b's bounded-memory proof exists as a real, runnable subprocess test independent of Phase 6's empirical benchmark, and it is honest about platform support — no silent pass on an unsupported platform.</done>
</task>

<task type="auto">
  <name>Task 3: `make verify-phase2` — the phase's own combined local gate (D-16g)</name>
  <files>Makefile</files>
  <read_first>
    Makefile (current state after 02-01/02-03's edits — `up down reset logs verify smoke-test
    generate fixtures fixtures-verify` targets should all already exist)
  </read_first>
  <action>
    Edit `Makefile`: add `verify-phase2` to the `.PHONY` line; add a `verify-phase2:` target
    running `uv run pytest tests/unit/ -x` followed by `$(MAKE) fixtures-verify` (D-16g — mirrors
    Phase 1's own `make verify`/`smoke-test` precedent of a single combined command a developer or
    later CI step can run). This is a LOCAL gate only — wiring it into GitHub Actions CI remains
    explicitly Phase 6's job (CI-01); this task does not touch any CI configuration. Run `make
    verify-phase2` once to confirm it is green end-to-end (config loader tests, config model tests,
    generator tests for both datasets, corpus manifest tests, bounded-memory test, and the full
    30-fixture digest-oracle verification, all in one command).
  </action>
  <verify>
    <automated>make verify-phase2</automated>
  </verify>
  <acceptance_criteria>
    - `make verify-phase2` exits 0.
    - `grep -n "verify-phase2:" Makefile` shows a target whose recipe invokes both `pytest` and `fixtures-verify` (directly or via `$(MAKE) fixtures-verify`).
  </acceptance_criteria>
  <done>Phase 2 is provably complete in one command: `make verify-phase2` runs the full unit-test suite (config, generator, corpus, bounded-memory) plus the fixture digest-oracle verification and exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `tools/corpus/generators.py`'s `wrapper` kind (gzip/zip) | Developer-authored, small, controlled compressed fixtures — not runtime-facing input in Phase 2's own scope, but the same compression formats a real decompression bomb would exploit in a future phase that decompresses untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 (canon: decompression-bomb shape, OWASP DoS-class — not minted as a must_haves.prohibition, breadcrumbed to `/gsd-secure-phase`) | Denial of Service | `29_gzip_wrapped_valid_file`, `30_zip_wrapped_valid_file` | low | accept | These fixtures are small (~20-row inner payload), developer-authored, and not decompressed by any runtime code path in Phase 2 — no real bomb-shaped ratio is authored. Phase 3, when it builds the real `compression.py` consumer, is the first phase to decompress untrusted-shaped input and owns bounding that decompression; flagged forward, not mitigated prematurely here |
| T-02-06 | Denial of Service / false-assurance (transparency) | `tests/unit/test_corpus_bounded_memory.py` | medium | mitigate | Explicit `pytest.skip(reason=...)` on a non-POSIX platform instead of a silent pass — backs this plan's `must_haves.prohibitions` entry directly |

</threat_model>

## Artifacts this phase produces

- **New `GeneratorKind` implementation** (`tools/corpus/generators.py`): the `"wrapper"` kind
  (gzip/zip dispatch, consuming an inline `format`/`inner` fixture spec).
- **New fixture entries** in `tests/fixtures/corpus.yaml` (category `large_compressed`):
  `28_large_streaming_profile`, `29_gzip_wrapped_valid_file`, `30_zip_wrapped_valid_file`.
- **`tests/fixtures/CORPUS.sha256`** extended from 27 to 30 entries (full corpus scope, D-16a).
- **New test module**: `tests/unit/test_corpus_bounded_memory.py`.
- **Makefile target**: `verify-phase2`.

<verification>
- `make verify-phase2` — the phase's single combined gate — exits 0.
</verification>

<success_criteria>
- The corpus reaches its full ~30-fixture scope across all 5 planned categories with a passing digest-oracle round-trip.
- D-16b's bounded-memory proof exists and is honest about platform support.
- `make verify-phase2` is green, closing out Phase 2 before `/gsd-verify-work` runs.
</success_criteria>

<output>
Create `.planning/phases/02-config-contract-csv-generator/02-05-SUMMARY.md` when done
</output>
