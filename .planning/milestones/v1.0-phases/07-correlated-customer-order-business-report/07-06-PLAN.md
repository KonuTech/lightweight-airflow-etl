---
phase: 07-correlated-customer-order-business-report
plan: 06
type: execute
wave: 5
depends_on: ["07-04", "07-05"]
files_modified:
  - docs/benchmark.md
  - docs/oracle.md
  - docs/csv-engine.md
  - README.md
  - Makefile
autonomous: true
requirements: [BENCH-01, DOC-02]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "docs/benchmark.md's speedup figure reflects a real measurement taken against the schema carrying the new PK/index/trigger overhead, not the stale pre-Phase-7 figure"
    - "README's Executive Summary business-report table shows genuine non-empty rows"
    - "docs/oracle.md and docs/csv-engine.md accurately describe the current customer_id/order_id generation scheme (structured, correlated) if they describe it at all"
    - "make verify-phase7 exists and mirrors verify-phase6's combined-gate shape"
  artifacts:
    - path: "docs/benchmark.md"
      provides: "Re-measured Run Metadata/Comparison Table/Speedup Ratio against the post-DDL schema (D-20)"
    - path: "README.md"
      provides: "Live-regenerated Executive Summary with a genuinely non-empty business-report table"
  key_links:
    - from: "Makefile verify-phase7"
      to: "verify-phase6's target shape"
      via: "unit -> e2e -> lint -> verify-evidence, requires make up"
      pattern: "verify-phase7:"
---

<objective>
Close out the phase's completion gate: re-measure the naive-vs-bulk benchmark against the schema
now carrying the new PK/index/trigger overhead (D-20), regenerate README's Executive Summary live
and confirm the business-report table is genuinely non-empty (Success Criterion 4), correct
`docs/oracle.md`/`docs/csv-engine.md` if either describes `customer_id` generation in a way that no
longer matches reality, and add `make verify-phase7` mirroring `verify-phase6`'s exact shape.

Purpose: This is the phase's own local completion gate and the last of the ROADMAP's 4 literal
success criteria (README reflects genuine non-empty results).
Output: Updated `docs/benchmark.md`, live-regenerated `README.md`, corrected docs if needed, `make
verify-phase7`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-correlated-customer-order-business-report/07-CONTEXT.md
@docs/benchmark.md
@docs/oracle.md
@docs/csv-engine.md
@Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure the benchmark against the post-DDL schema (D-20)</name>
  <files>docs/benchmark.md</files>
  <read_first>
    docs/benchmark.md (whole file — the exact Run Metadata / Comparison Table / Speedup Ratio /
    Per-Chunk Timing Breakdown sections and their table formats, all to be regenerated with fresh
    numbers in the same format); benchmark/run_benchmark.py (`--dataset` defaults to `customers`,
    line 49 — confirms the benchmark's dataset is unaffected by this phase's `orders_valid`-only
    trigger, but IS affected by the new `PRIMARY KEY`/implicit-index on `customers_valid` from Plan
    07-04); benchmark/naive_loader.py (unchanged, no action needed — reused as-is per RESEARCH.md).
  </read_first>
  <action>
    Run `uv run python -m benchmark.run_benchmark --mode naive --rows 100000 --seed 20260101` then
    `uv run python -m benchmark.run_benchmark --mode bulk --rows 100000 --seed 20260101` live
    against the current (post-Plan-07-04) stack. Capture the real output numbers (rows/sec, peak
    memory, Oracle load time, per-chunk timing, wall-clock). Update `docs/benchmark.md`'s Run
    Metadata table (new run date), Comparison Table, Speedup Ratio calculation, and Per-Chunk Timing
    Breakdown table with these fresh, real numbers — never fabricated or copied from the prior
    Phase 6 run. Add one sentence to the doc noting the schema now carries a `PRIMARY KEY` +
    implicit unique index on `customers_valid.customer_id` (Plan 07-04, D-20) and that this
    benchmark re-run reflects that overhead honestly, whatever direction it moves the numbers.
  </action>
  <verify>
    <automated>grep -c "rows/sec" docs/benchmark.md</automated>
  </verify>
  <precondition>`make up` stack already running and healthy; Plan 07-04's DDL already applied
  (customers_valid carries its new PRIMARY KEY)</precondition>
  <acceptance_criteria>
    - `docs/benchmark.md`'s Run Metadata table's "Run date" is updated to today's date
    - The Comparison Table's numbers differ from the pre-Phase-7 figures (a genuinely fresh
      measurement, not a copy-paste of the old one)
    - The Speedup Ratio calculation's arithmetic matches the newly-reported rows/sec figures
  </acceptance_criteria>
  <done>docs/benchmark.md reflects a real, current measurement against the schema carrying the new
  DDL overhead.</done>
</task>

<task type="auto">
  <name>Task 2: Live README regeneration proof, docs correction, verify-phase7 gate</name>
  <files>README.md, docs/oracle.md, docs/csv-engine.md, Makefile</files>
  <read_first>
    scripts/regenerate_readme_summary.py (after Plan 07-05 — the adapted `main()` this task runs
    live); docs/oracle.md and docs/csv-engine.md (grep-confirmed this session: neither currently
    describes `customer_id`/`order_id` as a random Faker word or otherwise contradicts the new
    correlated/structured scheme — `docs/oracle.md`'s only `customer_id` mentions are schema-shape
    references (line 22, 148, 152) that remain accurate; this task re-confirms via a fresh grep
    rather than trusting a stale finding, and corrects only if something IS found); Makefile (lines
    85-91, `verify-phase6`'s exact target shape — the template for the new `verify-phase7` target;
    line 1, `.PHONY:` list).
  </read_first>
  <action>
    Run `uv run python scripts/regenerate_readme_summary.py` live against the current stack
    (Plan 07-05's adapted version — correlated generation + staging/rename). Confirm the script
    exits 0 and `README.md`'s Executive Summary "Customers x Orders business report" table now shows
    genuine rows (not the placeholder "_(no rows yet...)_" line `_render_business_report_table()`
    emits when empty) — this is Success Criterion 4's live proof, not an assertion from code reading
    alone. Run `grep -n "customer_id" docs/oracle.md docs/csv-engine.md` and read any matches in
    context; correct only if a match describes generation as independently-random/Faker-word-based
    (contradicting the new correlated/structured reality) — otherwise leave both files untouched and
    note in the SUMMARY that neither needed correction. Add a `verify-phase7` target to `Makefile`
    mirroring `verify-phase6`'s exact shape (`uv run pytest tests/unit/ -x`, `uv run pytest
    tests/e2e/ -x`, `uv run pytest tests/integration/ -x`, `$(MAKE) lint`, `$(MAKE) verify-evidence`
    — Phase 7 adds the integration suite step `verify-phase6` didn't have, since `test_correlation_
    constraints.py` now lives under `tests/integration/`), with a `##` comment citing this phase's
    own requirements, and add `verify-phase7` to the `.PHONY:` line.
  </action>
  <verify>
    <automated>make verify-phase7</automated>
  </verify>
  <precondition>`make up` stack already running and healthy; Plans 07-01 through 07-05 already
  applied</precondition>
  <acceptance_criteria>
    - `README.md`'s Executive Summary business-report table contains at least one real data row
      (not the empty-state placeholder line)
    - `make verify-phase7` exists, is listed in `.PHONY:`, and passes end-to-end
    - `docs/oracle.md`/`docs/csv-engine.md` are either confirmed already-accurate (documented in the
      SUMMARY) or corrected — never silently left stale without at least a documented check
  </acceptance_criteria>
  <done>README's business report is live-proven non-empty; docs are confirmed accurate or
  corrected; make verify-phase7 is this phase's own combined local gate and passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Benchmark/regeneration scripts -> committed docs | Both scripts run locally against the dev stack; outputs are committed as documentation, not served to any external consumer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-06-01 | Repudiation | `docs/benchmark.md`/README numbers could be hand-edited instead of genuinely re-measured | low | accept | Both tasks require the actual live command output as their `<verify>`/`<acceptance_criteria>` — matches this project's existing "never fabricate benchmark numbers" discipline (Phase 6 precedent) |
</threat_model>

<verification>
- `docs/benchmark.md` reflects a fresh, real measurement against the post-DDL schema
- `README.md`'s Executive Summary business-report table is genuinely non-empty
- `make verify-phase7` passes (unit + e2e + integration + lint + verify-evidence)
</verification>

<success_criteria>
The phase's final completion gate passes locally: benchmark numbers are current, README reflects
real non-empty business-report evidence, docs are confirmed accurate, and `make verify-phase7`
green.
</success_criteria>

<artifacts_produced>
## Artifacts This Phase Produces (this plan's additions)

- `Makefile`: `verify-phase7` target (new)
- `docs/benchmark.md`: content updated with a fresh measurement (no new symbols)
- `README.md`: Executive Summary content regenerated (no new symbols; existing marker block
  structure from Phase 6 preserved)
</artifacts_produced>

<output>
Create `.planning/phases/07-correlated-customer-order-business-report/07-06-SUMMARY.md` when done
</output>
