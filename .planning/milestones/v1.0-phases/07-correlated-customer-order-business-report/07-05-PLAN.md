---
phase: 07-correlated-customer-order-business-report
plan: 05
type: execute
wave: 4
depends_on: ["07-02", "07-04"]
files_modified:
  - scripts/regenerate_readme_summary.py
  - tests/e2e/test_correlated_report_e2e.py
autonomous: true
requirements: [TEST-06, INFRA-04]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "scripts/regenerate_readme_summary.py generates both correlated CSVs via the one shared generate_correlated_datasets() function, never re-deriving correlation logic independently"
    - "Both correlated CSVs are staged then atomically renamed into their watched directories before the live csv_ingest DAG picks them up"
    - "The staging + atomic-rename mechanism is proven against the real, live Airflow stack, not mocked"
    - "The live e2e proof backdates some inserted orders_valid rows across multiple partition days and confirms the business report aggregates correctly across the partition boundary"
  artifacts:
    - path: "scripts/regenerate_readme_summary.py"
      provides: "_run_ingestion() adapted to consume a pre-generated, correlated GeneratedCsv and the staged-write-then-rename helper"
    - path: "tests/e2e/test_correlated_report_e2e.py"
      provides: "Second test proving D-24/D-25 (live staging+rename) and D-12 (multi-day backdated partitions) alongside Plan 07-01's first test"
  key_links:
    - from: "scripts/regenerate_readme_summary.py main()"
      to: "generator.generate_csv.generate_correlated_datasets()"
      via: "one call before the per-dataset trigger/wait loop"
      pattern: "generate_correlated_datasets\\("
    - from: "new e2e test"
      to: "csv_ingest DAG (live, HTTP-triggered)"
      via: "dag_polling.trigger_dag() -> wait_for_file deferred -> staged CSV rename -> wait_for_dag_run_result"
      pattern: "wait_for_task_state.*deferred"
---

<objective>
Prove D-23–D-25's full production mechanism live — generate both correlated CSVs first via the one
shared function, stage-then-atomically-rename each into its watched directory, and let the REAL,
already-proven `csv_ingest` DAG (Phase 5) pick each one up via its deferred `FileSensor` — and prove
D-12's multi-day backdated-partition aggregation. Adopt this same mechanism into
`scripts/regenerate_readme_summary.py` so the README's live evidence generation uses the identical
correlated, staged path (D-21/D-23), never a second independent implementation.

Purpose: Plan 07-01's tracer proved the correlation logic and the Oracle JOIN work by calling
`csv_processor.engine.process()` directly, deliberately bypassing Airflow to keep the tracer thin.
This plan closes that gap — the production path (both `make generate` callers AND the live e2e
suite) goes through the real HTTP-triggered DAG with the staging/rename mechanism, and proves the
business report aggregates correctly even when its underlying rows land in different daily
partitions.
Output: `scripts/regenerate_readme_summary.py` adopts `generate_correlated_datasets()` +
staging/rename; a second test in `tests/e2e/test_correlated_report_e2e.py` proves the live,
backdated-partition path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-correlated-customer-order-business-report/07-CONTEXT.md
@.planning/phases/07-correlated-customer-order-business-report/07-RESEARCH.md
@.planning/phases/07-correlated-customer-order-business-report/07-01-SUMMARY.md
@.planning/phases/07-correlated-customer-order-business-report/07-02-SUMMARY.md
@scripts/regenerate_readme_summary.py
@tests/e2e/test_correlated_report_e2e.py
@scripts/dag_polling.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adopt generate_correlated_datasets() + staging/rename in regenerate_readme_summary.py</name>
  <files>scripts/regenerate_readme_summary.py</files>
  <read_first>
    scripts/regenerate_readme_summary.py (whole file — `_run_ingestion()` lines 151-208 (its
    internal `generate_csv.generate_rows()` call and direct `generate_csv.write_csv()` call are the
    exact two lines this task replaces), `_DATASETS = ("customers", "orders")` line 62 (already the
    correct customers-then-orders order, D-23 needs no reordering), `main()`'s loop lines 342-344,
    `_clear_stale_fixtures()` lines 140-148 — still needed, called BEFORE generation, unchanged);
    generator/generate_csv.py (after Plan 07-01/07-02 — `generate_correlated_datasets()`,
    `CorrelatedDatasets`, the new `write_staged()`/`staging_path()` helpers from Plan 07-02).
  </read_first>
  <action>
    Restructure `scripts/regenerate_readme_summary.py`'s `main()`/`_run_ingestion()` per D-21/D-23:
    BEFORE the `for dataset in _DATASETS:` loop, load both dataset configs and call
    `generate_csv.generate_correlated_datasets(customers_config, orders_config,
    customers_rows=25, orders_rows=25, invalid_ratio=0.2, seed=<the existing run-unique
    time.time_ns()-derived seed, computed ONCE and shared across both datasets so D-05's
    determinism/pool-sampling stays internally consistent>)` — ONE call producing BOTH datasets'
    `GeneratedCsv` results, never per-dataset independent generation. Change `_run_ingestion(dataset:
    str)`'s signature to accept the pre-generated `GeneratedCsv` for that dataset (e.g.
    `_run_ingestion(dataset: str, generated: GeneratedCsv, config: DatasetConfig)`) instead of
    calling `generate_csv.generate_rows()` internally. Keep `_clear_stale_fixtures()` called first,
    exactly as today (still needed regardless of correlation). Replace the direct
    `generate_csv.write_csv(generated, config, fixture_path)` call inside `_run_ingestion()` with
    Plan 07-02's `generate_csv.write_staged(generated, config, dataset)` helper (D-24 — write to
    staging, then atomic rename into the watched path) — called ONLY AFTER `wait_for_task_state(...,
    "deferred")` confirms the sensor is genuinely waiting (Pitfall 4's existing ordering in this
    file is preserved exactly; only the WRITE call itself changes from direct-write to
    staged-write-then-rename). The `for dataset in _DATASETS:` loop then calls the adapted
    `_run_ingestion(dataset, correlated.customers if dataset == "customers" else correlated.orders,
    customers_config if dataset == "customers" else orders_config)` for each dataset independently
    (D-23 — generation is coupled, triggering/waiting stays decoupled and per-dataset).
  </action>
  <verify>
    <automated>uv run python -c "import ast; ast.parse(open('scripts/regenerate_readme_summary.py').read())" && grep -c "generate_correlated_datasets" scripts/regenerate_readme_summary.py</automated>
  </verify>
  <precondition>Plan 07-02's `write_staged()`/`staging_path()` helpers exist in
  generator/generate_csv.py</precondition>
  <acceptance_criteria>
    - `scripts/regenerate_readme_summary.py` calls `generate_correlated_datasets()` exactly once
      per `main()` invocation, before any per-dataset trigger/wait loop
    - `_run_ingestion()` no longer calls `generate_csv.generate_rows()` internally
    - `scripts/regenerate_readme_summary.py` calls `write_staged()` (not the bare `write_csv()`)
      for its production fixture write
  </acceptance_criteria>
  <done>regenerate_readme_summary.py uses the one shared correlation function and the one shared
  staging/rename helper — never a second independent implementation of either.</done>
</task>

<task type="auto">
  <name>Task 2: Live proof — staging+rename against the real Airflow stack, backdated multi-day partitions</name>
  <files>tests/e2e/test_correlated_report_e2e.py</files>
  <read_first>
    tests/e2e/test_correlated_report_e2e.py (Plan 07-01's own file and first test — this task ADDS a
    second test to the SAME file, D-10's "separate file" requirement is about
    test_csv_ingest_e2e.py, not about having only one test per file); airflow/dags/csv_ingest.py
    (whole file — the `wait_for_file` FileSensor's exact `filepath` template, line 84-94, confirming
    `.staging/` subdirectories are invisible to its non-recursive glob); scripts/dag_polling.py
    (`trigger_dag`, `wait_for_task_state`, `wait_for_dag_run_result`); packages/csv-processor/src/
    csv_processor/load.py (`insert_rows()` signature — this task calls it directly with an EXTENDED
    `columns` list including `"ingested_at"` to bypass `SYSDATE`'s default, per RESEARCH.md
    Pitfall 4).
  </read_first>
  <action>
    Add `test_correlated_ingestion_via_live_dag_trigger_reports_across_backdated_partitions` to
    `tests/e2e/test_correlated_report_e2e.py`. Follow `test_csv_ingest_e2e.py`'s exact
    poll-then-assert-then-act ordering, applied to BOTH datasets: (1) clear stale fixtures under
    both `data/customers/` and `data/orders/` (mirroring `_clear_existing_customers_fixtures()`'s
    shape, extended to `.staging/` subdirs too); (2) generate BOTH correlated CSVs via
    `generate_csv.generate_correlated_datasets(...)` with a run-unique seed; (3) trigger
    `csv_ingest` for `customers` via `dag_polling.trigger_dag(...)`, poll until `wait_for_file`
    reaches `"deferred"` (`timeout=180.0`, matching this project's established cold-start headroom);
    (4) ONLY THEN write the customers CSV via `generate_csv.write_staged(...)` (D-24's staging+
    rename, now exercised against the LIVE stack's actual watched directory — D-25); (5) repeat
    steps 3-4 for `orders`; (6) `dag_polling.wait_for_dag_run_result(...)` for both run ids, assert
    both succeed. Then, per RESEARCH.md Pitfall 4: call `csv_processor.load.insert_rows()` DIRECTLY
    (bypassing `process()`) with `columns=[<orders_config's column names in declared order>,
    "ingested_at"]` and 2-3 extra `orders_valid` rows whose `customer_id` values are drawn from the
    SAME correlated customers pool (reuse the pool this test already has from step 2) but whose
    `"ingested_at"` values are explicitly set to dates several days in the past (e.g. `date.today() -
    timedelta(days=2)` and `- timedelta(days=5)`) — proving D-12's multi-day backdated-partition
    coverage. Finally, execute the mirrored `_BUSINESS_REPORT_SQL` (same constant Plan 07-01 already
    defined in this file) and assert the result set includes rows corresponding to MORE THAN ONE
    distinct `order_month`/date bucket (proving the report aggregates correctly across the
    partition boundary the backdated rows create, not just today's partition).
  </action>
  <verify>
    <automated>uv run pytest tests/e2e/test_correlated_report_e2e.py -x</automated>
  </verify>
  <precondition>`make up` stack already running and healthy; Plan 07-04's PK/index/trigger DDL is
  already applied (Task 2's direct `insert_rows()` calls must supply real, pool-matched
  `customer_id` values or the new trigger will reject them — this is intentional and proves the
  trigger tolerates legitimate correlated backdated inserts too)</precondition>
  <acceptance_criteria>
    - `wait_for_file` reaches `"deferred"` for BOTH datasets BEFORE either staged file is renamed
      into its watched directory
    - The DAG run succeeds for both datasets
    - The backdated `insert_rows()` call succeeds (proves the trigger accepts a real, pool-matched
      `customer_id` regardless of `ingested_at`'s value)
    - The business report query returns rows spanning more than one distinct date/month bucket
  </acceptance_criteria>
  <done>D-24/D-25's staging+rename mechanism and D-12's multi-day partition aggregation are proven
  live, against the real Airflow stack, never mocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Staged file -> watched directory (host FS -> container bind mount) | Same-filesystem `rename(2)`, no network boundary; host process (test/script) writes, container process (FileSensor) reads |
| Test code -> orders_valid direct insert (bypassing process()) | Dev-only test code, real Oracle credentials (INFRA-03), used only to backdate `ingested_at` for partition-boundary coverage |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05-01 | Tampering | Direct `insert_rows()` call bypassing `csv_processor.engine.process()`'s normal validation | low | accept | Test-only code path, real Oracle credentials, dev-scope only (INFRA-03) — the bypassed validation (structural/type/nullability) is irrelevant here since the test constructs already-valid, already-typed row dicts itself; the DB-level trigger (Plan 07-04) still enforces the FK-existence guarantee on this path exactly as on every other insert path |
| T-07-05-02 | Denial of Service | A crashed test leaving an orphaned file under `data/<dataset>/.staging/` | low | accept | Same disposition as Plan 07-02's T-07-02-01 — invisible to the non-recursive FileSensor glob, no functional impact |
</threat_model>

<verification>
- `scripts/regenerate_readme_summary.py` parses and calls `generate_correlated_datasets()` exactly
  once per run
- `uv run pytest tests/e2e/test_correlated_report_e2e.py -x` passes both tests (Plan 07-01's direct-
  process proof and this plan's live-DAG/backdated-partition proof)
</verification>

<success_criteria>
The production write path (regenerate_readme_summary.py and the live e2e suite) generates both
correlated CSVs via the one shared function, stages then atomically renames each into its watched
directory, and is proven against the real Airflow stack with rows spanning multiple partition days.
</success_criteria>

<artifacts_produced>
## Artifacts This Phase Produces (this plan's additions)

- `scripts/regenerate_readme_summary.py`: `_run_ingestion()` signature changed to accept a
  pre-generated `GeneratedCsv`/`DatasetConfig`; `main()` calls `generate_correlated_datasets()` once
- `tests/e2e/test_correlated_report_e2e.py`: second test,
  `test_correlated_ingestion_via_live_dag_trigger_reports_across_backdated_partitions`
</artifacts_produced>

<output>
Create `.planning/phases/07-correlated-customer-order-business-report/07-05-SUMMARY.md` when done
</output>
