---
phase: 03-csv-processing-engine
plan: 05
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - tests/unit/test_engine_chunks.py
  - tests/unit/test_byte_level_hard.py
  - tests/unit/test_no_airflow_import.py
  - Makefile
autonomous: true
requirements: [ENGINE-01, ENGINE-04, ENGINE-05, ENGINE-07, ENGINE-09, TEST-01]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "process_chunks() yields multiple chunks for a file larger than one chunk_size, with row_number counting sequentially across chunk boundaries (never resetting to 1 at the start of a new chunk)."
    - "Streaming process_chunks() over the corpus's ~60 MiB large fixture survives a hard RLIMIT_AS address-space cap that a deliberately-broken whole-file-accumulating variant of the same call dies under — proving ENGINE-07's bounded-memory guarantee empirically, not just by code inspection."
    - "The 5 byte_level_hard corpus fixtures (23-27) parse correctly through process_chunks() via fixture-local ad hoc configs — embedded newline, embedded delimiter, doubled-quote escaping, and an embedded NUL byte all survive RFC-4180 parsing intact, and a 10,001-character field is not truncated."
    - "No file anywhere under packages/csv-processor/src/csv_processor/ imports anything from the airflow namespace, directly or transitively (ENGINE-09), verified by an AST-based scan, not a human read-through."
    - "`make verify-phase3` runs the complete Phase 3 unit test suite and is the single command a developer or CI runs to confirm the phase's own local gate (TEST-01)."
  artifacts:
    - tests/unit/test_no_airflow_import.py
    - tests/unit/test_byte_level_hard.py
    - Makefile (verify-phase3 target)
  key_links:
    - "tests/unit/test_corpus_bounded_memory.py's RLIMIT_AS/subprocess technique <-> this plan's process_chunks()-calling variant (same technique, new target function)"
    - "Makefile's verify-phase2 target <-> this plan's verify-phase3 target (identical convention, one phase later)"
    - "corpus fixtures 23-27 (byte_level_hard) <-> a fixture-local ad hoc DatasetConfig, never customers.json/orders.json directly (RESEARCH.md Pitfall 3 — their 2-column header doesn't match either real dataset's schema)"
  prohibitions:
    - statement: "MUST NOT read the entire file into memory at any point (e.g. `.readlines()`, `list(reader)`, accumulating every chunk's rows into one growing list) even temporarily for convenience — every read path must stay streaming/bounded, verified by the RLIMIT_AS test."
      status: unverified
      flagged: true
    - statement: "MUST NOT write a passing test that only checks 'it didn't crash' without asserting the actual expected error_code/typed-value/row-count — a green test suite must reflect genuine behavioral coverage, not just absence-of-crash."
      status: unverified
      flagged: true
</must_haves>
---

<objective>
Close out Phase 3's own test/verification surface: prove chunk-boundary and cross-chunk
`row_number` behavior explicitly (not just implicitly via the tracer's single-chunk case), prove
`process_chunks()`'s bounded-memory guarantee empirically against the corpus's real ~60 MiB
fixture using this project's own already-established `RLIMIT_AS`/subprocess technique, close the
one remaining corpus category no prior plan covers (`byte_level_hard`, fixtures 23-27 — RFC-4180
edge cases whose 2-column header doesn't match either real dataset, per RESEARCH.md Pitfall 3), add
the package-wide "no Airflow import" enforcement test ENGINE-09 requires, and wire the whole
phase's test suite behind one `make verify-phase3` command matching Phase 2's own `verify-phase2`
convention.

Purpose: this is the phase's own closing gate — after this plan, `make verify-phase3` is what a
developer (or a future CI job, Phase 6's CI-01) runs to confirm the whole `csv_processor` engine is
correct, and every one of the corpus's 30 fixtures (not just 25 of them) has been exercised by some
plan in this phase.
Output: extended `tests/unit/test_engine_chunks.py`, new `tests/unit/test_byte_level_hard.py`, new
`tests/unit/test_no_airflow_import.py`, `make verify-phase3`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-csv-processing-engine/03-CONTEXT.md
@.planning/phases/03-csv-processing-engine/03-RESEARCH.md
@tests/unit/test_corpus_bounded_memory.py
@Makefile
@tests/fixtures/corpus.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Chunk-boundary, cross-chunk row_number, and bounded-memory proof</name>
  <files>tests/unit/test_engine_chunks.py</files>
  <read_first>
    tests/unit/test_engine_chunks.py (03-03's tracer test already in this file — this task ADDS
    tests, does not modify the existing ones), tests/unit/test_corpus_bounded_memory.py (the exact
    `RLIMIT_AS`/subprocess technique this task reuses: `_RLIMIT_AS_BYTES` constant rationale,
    `_STREAMING_SCRIPT`/`_BUFFERING_SCRIPT` string-script pattern, `pytest.skip` on missing
    `resource` module, the `large_fixture_path` self-materializing fixture), tests/fixtures/corpus.yaml
    fixture 28 (`large_streaming_profile` — header `["id","payload"]`, matches NEITHER real
    dataset's schema per RESEARCH.md Pitfall 3, so this test builds its OWN ad hoc `DatasetConfig`
    matching fixture 28's actual 2-column shape rather than routing it through
    `customers.json`/`orders.json`)
  </read_first>
  <action>
    Add a chunk-boundary test: build a small ad hoc `DatasetConfig` (2 string columns, `chunk_size`
    override e.g. 5) via `DatasetConfig.model_validate({...})` directly (no file I/O needed for this
    one), write a literal 12-data-row CSV matching it to a temp file, call
    `list(process_chunks(path, config))`, and assert: exactly 3 chunks (`ceil(12/5)`), chunk sizes
    `[5, 5, 2]` (summed valid+invalid per chunk), and every yielded row's `row_number` forms the
    exact sequence `1..12` with NO resets at a chunk boundary (collect `row_number` from every
    invalid row across all 3 chunks — make at least a few of the 12 rows deliberately invalid, e.g.
    empty required fields, so `row_number` is directly inspectable without needing to also track it
    on valid rows).
    Add the bounded-memory proof, reusing `test_corpus_bounded_memory.py`'s exact
    `RLIMIT_AS`/subprocess technique: a `_STREAMING_SCRIPT` that imports `csv_processor.engine`,
    materializes fixture 28 via `tools.corpus.generators` (same self-materializing
    `large_fixture_path` fixture pattern), builds the ad hoc 2-column `DatasetConfig` (Claude's own
    judgment on `chunk_size`, e.g. 5000), calls `process_chunks(path, config)`, and for each yielded
    `(valid_rows, invalid_rows)` chunk ONLY increments running `total`/`valid`/`invalid` counters
    (`len(valid_rows)`, discarding the lists themselves immediately — never appending chunks to a
    growing list) — this is the module's actual bounded-memory CONTRACT, not just "iterate lines."
    A `_BUFFERING_SCRIPT` negative control does `list(process_chunks(path, config))` (accumulates
    every chunk's rows for the whole ~62,915-row file before ever summing anything) under the
    identical `RLIMIT_AS` cap. Start from `test_corpus_bounded_memory.py`'s own `_RLIMIT_AS_BYTES =
    25_165_824` (24 MiB) constant; if the streaming variant fails under that cap due to genuine
    per-row dict/typed-value construction overhead (not a bug — `process_chunks()` legitimately
    builds more per-row Python object weight than a raw line iterator), increase the constant only
    as much as empirically needed, re-confirming the buffering negative control still fails under
    the SAME new cap, and document the final chosen value and why directly in this test file's own
    header comment (mirroring `test_corpus_bounded_memory.py`'s own style) — never silently pick an
    arbitrarily generous cap that stops proving anything.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_engine_chunks.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The chunk-boundary test asserts exact chunk COUNT and exact per-chunk SIZE, not just "more
      than one chunk exists"
    - `row_number` values collected across all chunks form the literal sequence `1,2,3,...,12` with
      no duplicates and no resets
    - The bounded-memory streaming variant exits 0 under the chosen `RLIMIT_AS` cap; the buffering
      negative control exits non-zero (SIGKILL/MemoryError) under the SAME cap
    - `uv run pytest tests/unit/test_engine_chunks.py -x -q` exits 0
  </acceptance_criteria>
  <done>Chunk boundaries, cross-chunk row_number continuity, and the bounded-memory guarantee are
  all proven with real automated assertions against the actual ~60 MiB corpus fixture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RFC-4180 byte-level edge cases — corpus fixtures 23-27 (byte_level_hard)</name>
  <files>tests/unit/test_byte_level_hard.py</files>
  <read_first>
    tests/fixtures/corpus.yaml fixtures 23-27 (`byte_level_hard` category — embedded newline,
    embedded delimiter, doubled-quote escaping, embedded NUL byte, oversized field value; note their
    2-column `["order_id", "note"]`/`["order_id", "big_field"]` headers do NOT match either real
    dataset's schema), packages/csv-processor/src/csv_processor/source.py (03-03/03-04's
    `prepare_source` — this task calls it with a FIXTURE-LOCAL ad hoc `DatasetConfig`, never
    `customers.json`/`orders.json`, per RESEARCH.md Pitfall 3: routing these fixtures through the
    real dataset configs would trip a header-level `MISSING_REQUIRED_COLUMN`/`EXTRA_UNEXPECTED_COLUMN`
    whole-file reject before any of their actual byte-level content is ever reached)
  </read_first>
  <behavior>
    - Fixture 23 (`embedded_newline_in_quoted_field`): the `note` field's value, after parsing,
      equals the literal two-line string `"line one\nline two"` as ONE field — the embedded newline
      does not split it into two rows or two fields.
    - Fixture 24 (`embedded_delimiter_in_quoted_field`): `note` equals `"contains, a comma"` as one
      field — the embedded `,` does not split it into two fields.
    - Fixture 25 (`doubled_quote_escaping`): `note` equals `'she said "hi" again'` — the doubled
      `""` unescapes to one literal `"` each, not two.
    - Fixture 26 (`embedded_nul_byte`): parses without raising — Python's `csv.reader` accepts an
      embedded NUL byte in a field (verified live this session: `csv.reader` over a NUL-containing
      line returns the byte as a literal `"\x00"` character in the field string, no exception).
    - Fixture 27 (`oversized_field_value`): the 10,001-character `big_field` value parses
      successfully as a single field (well within 03-03's newly-set 1 MiB `csv.field_size_limit`) —
      this is a parser-robustness smoke test only, per RESEARCH.md's Open Question 2 resolution
      (no `ColumnSpec.max_length` field exists to drive a real "oversized value" error_code; treat
      this as "does the reader choke on a large-but-legitimate field", not a rejection case).
  </behavior>
  <action>
    For each of fixtures 23-27, materialize its bytes via `tools.corpus.generators` (same
    self-materializing pattern as prior plans), build a fixture-local ad hoc `DatasetConfig` matching
    its ACTUAL declared header (`order_id`/`note` for 23-25, `order_id`/`customer_id`/`order_date`/
    `amount` for 26, `order_id`/`big_field` for 27 — read each fixture's own `header:` list in
    `corpus.yaml` rather than assuming), run it through `csv_processor.engine.process_chunks(path,
    ad_hoc_config)`, and assert the exact field value in the `<behavior>` block above from the
    yielded `valid_rows[0]` dict — never just "no exception was raised." Document in this file's
    module docstring, matching `tests/unit/test_corpus_bounded_memory.py`'s own honesty convention,
    that these 5 fixtures are deliberately NOT run against `customers.json`/`orders.json` (would
    incorrectly whole-file-reject before reaching their actual RFC-4180 content), citing
    RESEARCH.md Pitfall 3 by name.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_byte_level_hard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All 5 fixtures (23-27) have a dedicated test asserting the EXACT recovered field value, not
      just "processing succeeded"
    - Fixture 26's test asserts no exception is raised AND that the recovered value contains the
      literal `"\x00"` character
    - `uv run pytest tests/unit/test_byte_level_hard.py -x -q` exits 0
  </acceptance_criteria>
  <done>All 30 of the corpus's fixtures (not just 25) have now been exercised by some plan in this
  phase — the byte_level_hard category's RFC-4180 edge cases are proven via fixture-local ad hoc
  configs, exactly as RESEARCH.md Pitfall 3 prescribes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ENGINE-09 enforcement — no Airflow import anywhere in csv_processor</name>
  <files>tests/unit/test_no_airflow_import.py</files>
  <read_first>
    packages/csv-processor/src/csv_processor/ (the full package tree this test scans — list its
    current files to confirm the scan covers every module this phase added: config/, detect/,
    errors.py, source.py, validate.py, normalize.py, engine.py, compression.py)
  </read_first>
  <behavior>
    - Scanning `packages/csv-processor/src/csv_processor/engine.py` (or any other real module in the
      package) finds zero `ast.Import`/`ast.ImportFrom` nodes whose module name is `"airflow"` or
      starts with `"airflow."`.
    - A synthetic test file containing `import airflow` (written to a temp `.py` file inside the
      test itself, never committed) IS correctly flagged by the same scanning function — proving the
      scanner isn't vacuously passing.
  </behavior>
  <action>
    Write a small `_imports_airflow(py_file: Path) -> bool` helper using `ast.parse` +
    `ast.walk` over the file's AST, checking every `ast.Import.names[*].name` and
    `ast.ImportFrom.module` for exact match `"airflow"` or a `"airflow."` prefix (covers both a bare
    `import airflow` and any `apache-airflow-providers-*` package, which installs under the
    `airflow.providers.*` namespace). Test 1: `for py_file in
    Path("packages/csv-processor/src/csv_processor").rglob("*.py"): assert not
    _imports_airflow(py_file), py_file` — one assertion per file so a failure names the exact
    offending file. Test 2 (self-test): write a temp file containing `"import airflow\n"`,
    assert `_imports_airflow(temp_file) is True` — proves the scanner has real detection power, not
    just a function that always returns `False`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_no_airflow_import.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The self-test (synthetic `import airflow` file) passes, proving the scanner actually detects
      what it claims to
    - Every real file under `packages/csv-processor/src/csv_processor/` passes with zero Airflow
      imports
    - `uv run pytest tests/unit/test_no_airflow_import.py -x -q` exits 0
  </acceptance_criteria>
  <done>ENGINE-09 is enforced by an automated, self-verifying test, not a one-time manual grep.</done>
</task>

<task type="auto">
  <name>Task 4: make verify-phase3 — Phase 3's combined local gate</name>
  <files>Makefile</files>
  <read_first>
    Makefile (the existing `verify-phase2` target's exact convention: `.PHONY` line, `##`
    trailing-comment self-documentation, `uv run pytest tests/unit/ -x` as the base command)
  </read_first>
  <action>
    Add `verify-phase3` to the `.PHONY` line and define it immediately after `verify-phase2`:
    `verify-phase3:     ## Phase 3's own combined local gate: full unit suite covering detect/
    compression/structural/type/nullability/chunking (TEST-01)` followed by `uv run pytest
    tests/unit/ -x` — matching `verify-phase2`'s exact style (a plain full-suite run; Phase 3
    introduces no new fixture-digest mechanism beyond Phase 2's already-committed
    `tests/fixtures/CORPUS.sha256`, so no `fixtures-verify` step is added here). Add a one-line
    comment above the target block, following the Makefile's own existing "Later phases (2-6) add
    targets here" comment convention, noting Phase 3's target follows the identical shape.
  </action>
  <verify>
    <automated>make verify-phase3</automated>
  </verify>
  <acceptance_criteria>
    - `make verify-phase3` exists, runs the full `tests/unit/` suite, and exits 0 with every test in
      this phase (03-02 through 03-05) passing
    - `.PHONY` line includes `verify-phase3`
  </acceptance_criteria>
  <done>`make verify-phase3` is Phase 3's single command for confirming the whole engine is correct,
  matching Phase 2's own established convention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test-only synthetic inputs | This plan's own test fixtures (chunk-boundary CSV, synthetic airflow-import file) are test-authored, not attacker-controlled — lowest-risk plan in the phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-12 | Tampering | A future contributor adding an `airflow` import to `csv_processor` without noticing ENGINE-09 | medium | mitigate | `test_no_airflow_import.py` runs on every `make verify-phase3` / future CI run, catching this before merge |
| T-03-13 | Denial of Service | A future change accidentally reintroducing whole-file buffering (e.g. `list(process_chunks(...))` in production code) | medium | mitigate | The bounded-memory RLIMIT_AS test is part of the standing suite, not a one-time manual check — it re-runs on every `make verify-phase3` |
</threat_model>

<verification>
Run `make verify-phase3` and confirm it exits 0.
</verification>

<success_criteria>
- Chunk boundaries and cross-chunk row_number continuity are proven with exact assertions
- process_chunks()'s bounded-memory guarantee is proven against the real ~60 MiB corpus fixture with
  a genuine negative control
- All 30 corpus fixtures (including the byte_level_hard category, 23-27) have been exercised by some
  plan in this phase
- ENGINE-09 (no Airflow import) is enforced by a self-verifying automated test
- `make verify-phase3` runs the complete Phase 3 unit suite green
</success_criteria>

<output>
Create `.planning/phases/03-csv-processing-engine/03-05-SUMMARY.md` when done.

## Artifacts this phase produces (this plan's contribution)
- `tests/unit/test_byte_level_hard.py` (fixtures 23-27 proof, fixture-local ad hoc configs)
- `tests/unit/test_no_airflow_import.py::_imports_airflow(py_file)` helper
- `make verify-phase3` Makefile target
</output>
