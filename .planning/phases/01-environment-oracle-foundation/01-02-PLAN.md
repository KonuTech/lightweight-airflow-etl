---
phase: 01-environment-oracle-foundation
plan: 2
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - docker/oracle/init/02_customers.sql
  - docker/oracle/init/03_orders.sql
  - scripts/verify_environment.py
autonomous: true
requirements: [INFRA-01]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "CUSTOMERS_VALID, CUSTOMERS_INVALID, ORDERS_VALID, ORDERS_INVALID tables exist in the ADMIN schema of FREEPDB1 with daily INTERVAL partitioning on INGESTED_AT, confirmed via USER_PART_TABLES/USER_TAB_PARTITIONS."
    - "scripts/verify_environment.py confirms all 5 tables (4 data tables + INGESTION_METADATA) and their expected columns exist, via USER_TABLES and ALL_TAB_COLUMNS — matching ROADMAP.md Phase 1 Success Criterion 2 exactly."
  artifacts:
    - path: "docker/oracle/init/02_customers.sql"
      provides: "CUSTOMERS_VALID/CUSTOMERS_INVALID DDL, D-01 column shape, D-03 INTERVAL partitioning"
    - path: "docker/oracle/init/03_orders.sql"
      provides: "ORDERS_VALID/ORDERS_INVALID DDL, D-01 column shape, D-03 INTERVAL partitioning"
  key_links:
    - from: "docker/oracle/init/02_customers.sql, docker/oracle/init/03_orders.sql"
      to: "scripts/verify_environment.py's expected-table set"
      via: "table name list literal match"
      pattern: "CUSTOMERS_VALID|CUSTOMERS_INVALID|ORDERS_VALID|ORDERS_INVALID"
  prohibitions: []
---

<objective>
Expand the Oracle schema from Plan 01-01's single unpartitioned table to the full 5-table schema
required by ROADMAP.md Phase 1 Success Criterion 2: CUSTOMERS_VALID, CUSTOMERS_INVALID,
ORDERS_VALID, ORDERS_INVALID (all four INTERVAL-partitioned per D-03) plus the already-existing
INGESTION_METADATA. Extend scripts/verify_environment.py so it verifies the complete set, per D-05,
via Oracle's own USER_TABLES/ALL_TAB_COLUMNS dictionary views — not just DDL exit status.

Purpose: Phase 2's config.json contract will be written to match these exact tables (D-01); getting
the column shape and partitioning right now, verified against a real running Oracle instance,
avoids a costly re-migration later (D-01's own "costly" rationale).

Output: All 5 target tables live and verified in ADMIN/FREEPDB1, reusable by Phase 4's
`csv_processor` bulk-load tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-environment-oracle-foundation/01-CONTEXT.md
@.planning/phases/01-environment-oracle-foundation/01-RESEARCH.md
@.planning/phases/01-environment-oracle-foundation/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CUSTOMERS_VALID/INVALID and ORDERS_VALID/INVALID DDL with INTERVAL partitioning</name>
  <reversibility rating="one-way">D-03 (already locked, exact syntax) — converting a partitioned table's partitioning scheme after data exists requires a partition-exchange/rebuild migration, not a simple DDL edit. This decision was fully specified by the user in CONTEXT.md D-03 (INTERVAL partitioning, daily, on an ingestion-date column) and empirically verified in RESEARCH.md this session — the door is already walked through, so no checkpoint is inserted per the "decision already made" rule; the rating is recorded here for the record.</reversibility>
  <files>docker/oracle/init/02_customers.sql, docker/oracle/init/03_orders.sql</files>
  <read_first>
    .planning/phases/01-environment-oracle-foundation/01-RESEARCH.md (Pattern 2, Pattern 3 — exact verified DDL syntax and the p_initial pitfall)
    .planning/phases/01-environment-oracle-foundation/01-PATTERNS.md (docker/oracle/init/01_customers.sql and 02_orders.sql sections — column shape source)
    .planning/phases/01-environment-oracle-foundation/01-CONTEXT.md (D-01, D-02, D-03)
    docker/oracle/init/01_ingestion_metadata.sql (from Plan 01-01 — the ALTER SESSION preamble pattern to repeat exactly)
  </read_first>
  <action>
    Create `docker/oracle/init/02_customers.sql`. Preamble (Pattern 2, no exceptions):
    `ALTER SESSION SET CONTAINER = FREEPDB1;` then `ALTER SESSION SET CURRENT_SCHEMA = ADMIN;`.
    `CREATE TABLE customers_valid` with columns: `CUSTOMER_ID VARCHAR2(64) NOT NULL`, `NAME
    VARCHAR2(255) NOT NULL`, `COUNTRY VARCHAR2(64) NOT NULL`, `BIRTH_DATE DATE`, `EVENT_TS
    TIMESTAMP WITH TIME ZONE NOT NULL`, `SIGNUP_COUNTRY VARCHAR2(64)`, `INGESTED_AT DATE DEFAULT
    SYSDATE NOT NULL` (the partition key — D-01 strips SCD-era `business_key`/`scd_type`
    annotations from the reference repo's customers.yaml shape). Then `PARTITION BY RANGE
    (INGESTED_AT) INTERVAL (NUMTODSINTERVAL(1, 'DAY')) (PARTITION p_initial VALUES LESS THAN (DATE
    '2020-01-01'))` — the exact verified syntax from RESEARCH.md Pattern 3, including the
    mandatory named initial partition (a bare interval clause with zero partitions errors).
    `CREATE TABLE customers_invalid` with the identical column set plus `ERROR_CODE VARCHAR2(64)
    NOT NULL`, `ERROR_MESSAGE VARCHAR2(4000) NOT NULL`, `SOURCE_FILE VARCHAR2(255) NOT NULL`,
    `ROW_NUMBER NUMBER NOT NULL` (D-01/ENGINE-06), same partitioning clause.

    Create `docker/oracle/init/03_orders.sql` with the identical `ALTER SESSION` preamble.
    `CREATE TABLE orders_valid` with columns: `ORDER_ID VARCHAR2(64) NOT NULL`, `CUSTOMER_ID
    VARCHAR2(64) NOT NULL`, `ORDER_DATE DATE`, `AMOUNT NUMBER(12,2)`, `INGESTED_AT DATE DEFAULT
    SYSDATE NOT NULL` (partition key), same `PARTITION BY RANGE (INGESTED_AT) INTERVAL
    (NUMTODSINTERVAL(1, 'DAY')) (PARTITION p_initial VALUES LESS THAN (DATE '2020-01-01'))` clause.
    `CREATE TABLE orders_invalid` with the identical column set plus `ERROR_CODE VARCHAR2(64) NOT
    NULL`, `ERROR_MESSAGE VARCHAR2(4000) NOT NULL`, `SOURCE_FILE VARCHAR2(255) NOT NULL`,
    `ROW_NUMBER NUMBER NOT NULL`, same partitioning clause.

    All identifiers plain uppercase, unquoted (D-02) — do not wrap any table/column name in double
    quotes anywhere in either file.
  </action>
  <verify>
    <automated>
      docker compose down -v
      docker compose up -d --wait
      printf "SELECT table_name, partitioning_type FROM user_part_tables WHERE table_name IN ('CUSTOMERS_VALID','CUSTOMERS_INVALID','ORDERS_VALID','ORDERS_INVALID') ORDER BY table_name;\nexit;\n" | docker compose exec -T oracle sqlplus -s admin/admin@localhost:1521/FREEPDB1 | grep -c RANGE
    </automated>
  </verify>
  <acceptance_criteria>
    - `docker compose down -v && docker compose up -d --wait` succeeds from a clean volume state
      (proves the init scripts run correctly on genuine first boot, not just an already-initialized
      volume)
    - The `user_part_tables` query returns `RANGE` partitioning type for all 4 tables (4 matches)
    - `docker exec` into oracle and running `SELECT owner FROM all_tables WHERE table_name =
      'CUSTOMERS_VALID'` returns `ADMIN`, not `SYS`
  </acceptance_criteria>
  <done>All 4 data tables exist, correctly partitioned, in the ADMIN schema.</done>
</task>

<task type="auto">
  <name>Task 2: Extend verify_environment.py to the full 5-table + column set</name>
  <files>scripts/verify_environment.py</files>
  <read_first>
    scripts/verify_environment.py (from Plan 01-01 — the existing verify_tables() function to extend)
    .planning/phases/01-environment-oracle-foundation/01-RESEARCH.md (D-05 code example, full expected set)
  </read_first>
  <action>
    Update the `expected` set passed to `verify_tables()` in `scripts/verify_environment.py`'s
    `__main__` block to all 5 tables: `CUSTOMERS_VALID`, `CUSTOMERS_INVALID`, `ORDERS_VALID`,
    `ORDERS_INVALID`, `INGESTION_METADATA`. Add a second reusable function
    `verify_columns(cursor, table: str, expected_columns: set[str]) -> None` querying
    `all_tab_columns` filtered on `table_name = :table AND owner = 'ADMIN'`, asserting the found
    column-name set is a superset of `expected_columns` (superset, not exact-equal, so adding a
    column later doesn't break this check). Call it for `CUSTOMERS_VALID` with at least
    `{"CUSTOMER_ID", "NAME", "COUNTRY", "INGESTED_AT"}` and for `ORDERS_VALID` with at least
    `{"ORDER_ID", "CUSTOMER_ID", "AMOUNT", "INGESTED_AT"}` as a representative column-shape check
    (full column parity is enforced by Task 1's DDL itself, not re-derived here). Keep both new
    checks additive to the existing `verify_tables()`/`verify_airflow_auth()` calls — do not remove
    or weaken Plan 01-01's checks.
  </action>
  <verify>
    <automated>uv run python scripts/verify_environment.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python scripts/verify_environment.py` exits 0 against the full 5-table schema
    - Script output shows a distinct pass line for the table-existence check and the column check
    - Deleting `docker/oracle/init/02_customers.sql` and re-running `docker compose down -v &&
      docker compose up -d --wait && uv run python scripts/verify_environment.py` fails with a
      clear `AssertionError` naming `CUSTOMERS_VALID`/`CUSTOMERS_INVALID` as missing (confirms the
      check is real, not vacuous) — run this as a one-off sanity check, then restore the file
  </acceptance_criteria>
  <done>verify_environment.py verifies all 5 tables and representative columns for both data tables.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Oracle init scripts -> ADMIN schema | DDL-only, no application-facing input surface exists yet at this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | `docker/oracle/init/02_customers.sql`, `03_orders.sql` running as SYS before the ALTER SESSION preamble | medium | mitigate | Every init script explicitly runs `ALTER SESSION SET CONTAINER = FREEPDB1; ALTER SESSION SET CURRENT_SCHEMA = ADMIN;` before any CREATE TABLE (Pattern 2), preventing silent table creation under SYS/CDB$ROOT — verified via Task 1's acceptance criteria checking `all_tables.owner` |
</threat_model>

<verification>
`docker compose down -v && docker compose up -d --wait` succeeds from a genuinely clean volume.
`uv run python scripts/verify_environment.py` exits 0, confirming all 5 tables, their partitioning,
and representative columns.
</verification>

<success_criteria>
ROADMAP.md Phase 1 Success Criterion 2 is fully met: all 5 tables exist for both datasets,
confirmed by querying Oracle's own metadata/dictionary views.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

- `docker-compose.yml` — full service topology (Plan 01-01; Plan 01-03 edits it)
- `docker/airflow/Dockerfile` — custom Airflow image (Plan 01-03)
- `docker/oracle/init/*.sql` — schema DDL (Plan 01-01: `01_ingestion_metadata.sql`; this plan: `02_customers.sql`, `03_orders.sql`)
- `scripts/verify_environment.py` — D-05 verification script (Plan 01-01 creates it; this plan extends it)
- `Makefile` — lifecycle commands (Plan 01-04)
- `docs/environment.md` — resource + networking docs (Plan 01-04)
- `.env.example` — credential template (Plan 01-01; Plan 01-03 extends it)
- `packages/csv-processor/src/csv_processor/` — empty package scaffold, no logic yet (Plan 01-03)
- `airflow/dags/` — empty scaffold dir, no DAG code yet (Plan 01-03)
</artifacts_produced>

<output>
Create `.planning/phases/01-environment-oracle-foundation/01-02-SUMMARY.md` when done
</output>
