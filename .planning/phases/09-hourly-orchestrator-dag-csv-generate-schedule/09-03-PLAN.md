---
phase: 09-hourly-orchestrator-dag-csv-generate-schedule
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - Makefile
  - docs/airflow-dag.md
autonomous: true
requirements: [SCHED-01, SCHED-04, SCHED-05, SCHED-08]

must_haves:
  truths:
    - "make verify-phase9 is a permanent, committed check that structurally proves schedule=\"@hourly\", catchup=False, max_active_runs=1, all three trigger tasks' deferrable/fail_when_dag_is_paused flags, and the rows/invalid_ratio Param defaults against a live, running Airflow container (SCHED-01/SCHED-04/SCHED-05/SCHED-08)"
    - "docs/airflow-dag.md documents csv_generate_schedule's task graph and per-task role in the same style as the existing csv_ingest/report_ready sections, so a future reader/operator can understand the hourly cascade without reading source"
  artifacts:
    - path: "Makefile"
      provides: "verify-phase9 target following the verify-phase5..8 convention"
      contains: "verify-phase9:"
    - path: "docs/airflow-dag.md"
      provides: "## `csv_generate_schedule` DAG section: task graph, per-task bullets, Live Verification Evidence placeholder"
      contains: "## `csv_generate_schedule` DAG"
  key_links:
    - from: "Makefile's verify-phase9 target"
      to: "airflow/dags/csv_generate_schedule.py (live, inside the airflow-scheduler container)"
      via: "docker compose exec -T airflow-scheduler python -c \"...BundleDagBag...\""
      pattern: "b\\.dags\\['csv_generate_schedule'\\]"
---

<objective>
Add the permanent `make verify-phase9` structural regression check (mirroring
`verify-phase5`/`verify-phase8`'s exact `BundleDagBag`-based shape) and document the new DAG in
`docs/airflow-dag.md`, following the existing `csv_ingest`/`report_ready` section conventions.

Purpose: A committed, repeatable local gate proving SCHED-01/04/05/08's structural properties
against the real running container -- not just the static ruff/mypy checks Plan 09-02 already ran.
Output: `Makefile`'s `verify-phase9` target; `docs/airflow-dag.md`'s new `csv_generate_schedule`
section (evidence populated by Plan 09-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-hourly-orchestrator-dag-csv-generate-schedule/09-CONTEXT.md
@.planning/phases/09-hourly-orchestrator-dag-csv-generate-schedule/09-RESEARCH.md
@.planning/phases/09-hourly-orchestrator-dag-csv-generate-schedule/09-02-SUMMARY.md
</context>

<interfaces>
<!-- Makefile's existing verify-phase5 target -- exact shape verify-phase9 must mirror -->
```makefile
verify-phase5:     ## Phase 5's own combined local gate: unit suite + live DagBag structure check (requires `make up` first)
	uv run pytest tests/unit/ -x
	docker compose exec -T airflow-scheduler python -c "\
from pathlib import Path; \
from airflow.dag_processing.dagbag import BundleDagBag; \
b = BundleDagBag(bundle_path=Path('/opt/airflow/dags'), dag_folder='/opt/airflow/dags'); \
assert not b.import_errors, b.import_errors; \
dag = b.dags['csv_ingest']; \
required = {'load_config_task','wait_for_file','process_csv_task','load_results_task','report_result_task'}; \
assert required.issubset(set(dag.task_ids)), dag.task_ids; \
assert dag.get_task('wait_for_file').deferrable is True; \
print('DAGBAG_OK')"
```

<!-- docs/airflow-dag.md's existing report_ready DAG section -- exact shape to mirror -->
```markdown
## `report_ready` DAG

`airflow/dags/report_ready.py` (D-26) is dataset-agnostic ...

### Task Graph

```
wait_for_both_datasets -> build_report_task
```

- `wait_for_both_datasets` -- a `ReportReadySensor` ...
- `build_report_task` -- a plain `@task` that opens a normal (blocking) Oracle connection ...

### Live Verification Evidence
...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: verify-phase9 Makefile target</name>
  <files>Makefile</files>
  <read_first>
    Makefile (full file -- verify-phase5's/verify-phase8's exact shape, `.PHONY` line 1)
    airflow/dags/csv_generate_schedule.py (Plan 09-02's completed file -- exact task_ids to
    assert: generate_task, trigger_customers, trigger_orders, trigger_report_ready, summary_task,
    retention_task; the exact `Param` default values 100/0.1)
    .planning/phases/09-hourly-orchestrator-dag-csv-generate-schedule/09-RESEARCH.md (Code
    Examples section 6 "verify-phase9 Makefile target" -- the exact BundleDagBag assertion shape
    to reproduce and extend)
  </read_first>
  <action>
    Add `verify-phase9` to the `.PHONY:` declaration on line 1, alongside the existing
    `verify-phase8` entry. Add a new `verify-phase9:` target below `verify-phase8`, with a
    `##`-suffixed description matching the existing `verify-phaseN` convention ("Phase 9's own
    combined local gate: unit suite + live DagBag structure check (requires `make up` first)").
    Recipe: first `uv run pytest tests/unit/ -x`, then a `docker compose exec -T
    airflow-scheduler python -c "..."` block using `BundleDagBag(bundle_path=Path('/opt/airflow/dags'),
    dag_folder='/opt/airflow/dags')`, asserting: `not b.import_errors`; `dag =
    b.dags['csv_generate_schedule']`; `required = {'generate_task','trigger_customers',
    'trigger_orders','trigger_report_ready','summary_task','retention_task'}` is a subset of
    `dag.task_ids`; `dag.max_active_runs == 1` (SCHED-04); `dag.get_task('trigger_customers').deferrable
    is True` and `dag.get_task('trigger_customers').fail_when_dag_is_paused is True` (SCHED-05,
    checked for at least `trigger_customers` -- the other two trigger tasks use identical literal
    values per Plan 09-02); `dag.params['rows'].value == 100` and
    `dag.params['invalid_ratio'].value == 0.1` (SCHED-08); print `DAGBAG_OK` on success -- mirroring
    `verify-phase5`'s exact `BundleDagBag` shape (`Makefile` lines 64-75), swapping
    `dag_ids['csv_ingest']` for `dag_ids['csv_generate_schedule']` and the required-task-id set per
    RESEARCH.md's Code Examples section 6.
  </action>
  <acceptance_criteria>
    - `grep -c "verify-phase9" Makefile` is at least 2 (`.PHONY` entry + target definition).
    - `grep -c "^verify-phase9:" Makefile` equals 1 (exactly one target definition).
    - `docker compose up -d --wait && make verify-phase9` exits 0 and prints `DAGBAG_OK`.
  </acceptance_criteria>
  <verify>
    <automated>docker compose up -d --wait && make verify-phase9</automated>
  </verify>
  <done>`make verify-phase9` is a permanent, committed target; running it against the live stack
  (post `docker compose up -d --wait`) prints `DAGBAG_OK` with zero `import_errors` and confirms
  every SCHED-01/04/05/08 structural assertion.</done>
</task>

<task type="auto">
  <name>Task 2: docs/airflow-dag.md -- new csv_generate_schedule section</name>
  <files>docs/airflow-dag.md</files>
  <read_first>
    docs/airflow-dag.md (full file -- the exact `## csv_ingest DAG` / `## report_ready DAG`
    section shape, task-graph fenced-block + bullet-list convention, the top-of-file intro
    paragraph naming both existing DAGs)
    airflow/dags/csv_generate_schedule.py (Plan 09-02's completed file -- the authoritative task
    graph/behavior to document)
  </read_first>
  <action>
    Add a new `## \`csv_generate_schedule\` DAG` section to `docs/airflow-dag.md`, positioned
    after the existing `## \`report_ready\` DAG` section (before `## Full HTTP-to-Oracle-rows
    automated testing`), following that section's exact shape: a one-paragraph intro (states
    `@hourly`/`catchup=False`/`max_active_runs=1`, that it chain-triggers the existing, unmodified
    `csv_ingest`/`report_ready` DAGs, and that `rows`/`invalid_ratio` are operator-overridable DAG
    `Param`s per SCHED-08), a fenced `### Task Graph` block reading `generate_task ->
    trigger_customers -> trigger_orders -> trigger_report_ready -> summary_task ->
    retention_task`, then one bullet per task in that same order explaining its role in the same
    terse style as the `csv_ingest`/`report_ready` bullet lists (mention D-04's
    `logical_date`-derived seed for `generate_task`, D-06/D-07/D-08's deterministic
    `trigger_run_id`/`skip_when_already_exists`/`deferrable` for the three trigger tasks,
    D-12/D-13/D-14's direct-Oracle-query for `summary_task`, D-16/D-18's 30-day/best-effort
    contract for `retention_task`). Add a `### Live Verification Evidence` subsection heading with
    placeholder text `(populated by 09-04-PLAN.md's live-triggered run)` -- the actual evidence
    content is out of scope for this task, added by the next plan.

    Also update this file's top-of-document intro paragraph (the sentence currently naming "the
    config-driven `csv_ingest` DAG... and the `report_ready` DAG") to add a third clause naming
    `csv_generate_schedule` as the hourly orchestrator that chain-triggers both.
  </action>
  <acceptance_criteria>
    - `grep -c '## \`csv_generate_schedule\` DAG' docs/airflow-dag.md` equals 1.
    - `grep -c "generate_task -> trigger_customers -> trigger_orders -> trigger_report_ready ->
      summary_task -> retention_task" docs/airflow-dag.md` equals 1.
    - `grep -c "### Live Verification Evidence" docs/airflow-dag.md` equals 3 (existing
      `csv_ingest` section, existing `report_ready` section, and this new one).
    - `grep -c "csv_generate_schedule" docs/airflow-dag.md` is at least 5 (intro paragraph, section
      heading, task graph line, and per-task bullet references).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "csv_generate_schedule" docs/airflow-dag.md</automated>
  </verify>
  <done>`docs/airflow-dag.md` documents `csv_generate_schedule`'s task graph and per-task role in
  the same style as the two existing DAG sections, with a placeholder for live evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local developer machine ↔ `docker compose exec` subprocess call | `make verify-phase9` shells out from the developer's own host process into a running container; invoked only locally, never over a network -- same boundary Phase 8's `T-08-05`..`T-08-07` already documented for `verify-phase8` |
| Verification output ↔ terminal/CI log | The exec'd Python one-liner prints only fixed structural facts (task IDs, boolean flags, Param defaults); no secrets read or interpolated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-9-07 | Information Disclosure | `verify-phase9`'s exec'd Python one-liner / captured stdout | accept | Contains no secrets -- only DAG structural metadata (task IDs, boolean operator flags, Param default values), consistent with `verify-phase8`'s `T-08-06` precedent. |
| T-9-08 | Elevation of Privilege | `docker compose exec` invoked by `make verify-phase9` | accept | Runs with the invoking developer's own local Docker permissions, not elevated -- matches `verify-phase5`/`verify-phase8`'s existing precedent, which already exec into a container the same way. |

</threat_model>

<verification>
1. `docker compose up -d --wait && make verify-phase9` exits 0, prints `DAGBAG_OK`.
2. `grep -c '## \`csv_generate_schedule\` DAG' docs/airflow-dag.md` equals 1.
3. `grep -c "### Live Verification Evidence" docs/airflow-dag.md` equals 3.
</verification>

<success_criteria>
- `make verify-phase9` is a permanent, committed regression check proving SCHED-01/04/05/08
  against the live container.
- `docs/airflow-dag.md` documents the new DAG's task graph and per-task role, ready for Plan
  09-04 to fill in live evidence.
</success_criteria>

<output>
Create `.planning/phases/09-hourly-orchestrator-dag-csv-generate-schedule/09-03-SUMMARY.md` when done
</output>
