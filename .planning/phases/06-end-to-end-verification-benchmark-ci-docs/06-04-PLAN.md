---
phase: 06-end-to-end-verification-benchmark-ci-docs
plan: 04
type: execute
wave: 2
depends_on: ["06-01", "06-03"]
files_modified:
  - scripts/verify_evidence.sql
  - scripts/regenerate_readme_summary.py
  - README.md
  - .github/workflows/readme-summary.yml
autonomous: true
requirements: [TEST-03, DOC-01]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Evidence of a working pipeline is captured by directly querying Oracle via a committed, reproducible SQL script (D-09) — never an ad hoc/undocumented manual sqlplus session."
    - "The evidence includes a real customers-JOIN-orders business report, aggregated by region (customers.country, the explicitly-flagged proxy for the absent literal region column) and month-of-order_date, with order count + total/average amount (D-10)."
    - "README.md carries a top Executive Summary (D-11) with real row counts, a deferred-wake proof line, and the business report's top-N rows — regenerated live via CI auto-commit on every merge to main (D-12), using the default GITHUB_TOKEN (never a PAT, Pitfall 2) so the commit structurally cannot re-trigger CI."
  artifacts:
    - scripts/verify_evidence.sql
    - scripts/regenerate_readme_summary.py
    - .github/workflows/readme-summary.yml
  key_links:
    - "scripts/regenerate_readme_summary.py -> scripts/dag_polling.py (Plan 01's reusable trigger/poll helpers, not re-derived)"
    - "scripts/regenerate_readme_summary.py -> scripts/verify_evidence.sql (the business-report query text is mirrored, never re-authored independently, so the two never silently diverge)"
    - "readme-summary.yml -> docker-compose.yml (same compose-up bring-up pattern as ci.yml's oracle-e2e job, Pattern 4 mirrors Pattern 3)"
  prohibitions:
    - statement: "The Executive Summary regeneration (readme-summary.yml / regenerate_readme_summary.py) must never silently fail, loop, or leave stale/misleading 'proof of a working platform' content in README.md without a visible failure signal — a failed regeneration must fail the workflow run loudly (non-zero exit), never swallow the error and leave old/wrong content in place uncommitted-but-unflagged."
      status: unresolved
      flagged: true
  assumptions: []
---

<objective>
Build the reproducible Oracle evidence-capture script (D-09), the customers-JOIN-orders business
report (D-10), and the live-regenerated README Executive Summary (D-11/D-12/D-13) — automated via
a push-to-main-only GitHub Actions job that re-runs a real ingestion, captures the deferred-wake
proof, queries Oracle, and commits the refreshed section back using the default `GITHUB_TOKEN`
(never a PAT, per Pitfall 2's structural loop-prevention).

Purpose: D-09 through D-13 are user-initiated scope additions that fold TEST-03's "prove it works"
and DOC-01's "documented" requirements into one visible, reproducible piece of evidence at the top
of the README, not a one-time manual capture.
Output: `scripts/verify_evidence.sql`, `scripts/regenerate_readme_summary.py`, README.md's
Executive Summary section (seeded with real evidence from this plan's own execution), and
`.github/workflows/readme-summary.yml`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-end-to-end-verification-benchmark-ci-docs/06-CONTEXT.md
@.planning/phases/06-end-to-end-verification-benchmark-ci-docs/06-RESEARCH.md
@scripts/dag_polling.py
@scripts/trigger_dag.sh
@.github/workflows/ci.yml
@docker/oracle/init/02_customers.sql
@docker/oracle/init/03_orders.sql
@README.md
@generator/generate_csv.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/verify_evidence.sql — reproducible row-count + business-report evidence (D-09/D-10)</name>
  <files>scripts/verify_evidence.sql</files>
  <precondition>The docker-compose stack (`make up`) is already running with at least one prior successful ingestion recorded (e.g. from Plan 01's e2e test run) so the queries return non-empty, real evidence when this task verifies itself.</precondition>
  <read_first>
    docker/oracle/init/02_customers.sql, docker/oracle/init/03_orders.sql (exact column
    names/types: `customers_valid.customer_id`/`.country`, `orders_valid.customer_id`/
    `.order_date`/`.amount` — both `_valid` tables, never the widened-to-VARCHAR2 `_invalid`
    tables, for the business report); docker/oracle/init/01_ingestion_metadata.sql (`dataset`,
    `file_name`, `checksum`, `total_rows`, `valid_rows`, `invalid_rows`, `status`,
    `processed_at` columns — the source for D-11a's per-dataset row counts).
  </read_first>
  <action>
    Write `scripts/verify_evidence.sql` with a header comment citing D-09/D-10, then exactly
    two `SELECT` statements under `PROMPT` headers: (1) the latest `ingestion_metadata`
    record per dataset (`dataset`, `file_name`, `checksum`, `total_rows`, `valid_rows`,
    `invalid_rows`, `status`, `processed_at`, filtered to
    `processed_at = (SELECT MAX(processed_at) FROM ingestion_metadata im2 WHERE im2.dataset =
    im.dataset)`, ordered by `dataset`); (2) the customers-JOIN-orders business report:
    `SELECT c.country AS region, TRUNC(o.order_date, 'MM') AS order_month, COUNT(*) AS
    order_count, SUM(o.amount) AS total_amount, ROUND(AVG(o.amount), 2) AS avg_amount FROM
    customers_valid c JOIN orders_valid o ON o.customer_id = c.customer_id GROUP BY
    c.country, TRUNC(o.order_date, 'MM') ORDER BY region, order_month` — a read-only JOIN on
    `customer_id` (D-10 explicitly does not reopen the "FK not enforced" out-of-scope
    decision; this is reporting evidence, not a referential-integrity validator). Add
    `SET PAGESIZE`/`SET LINESIZE`/`COLUMN ... FORMAT` directives for readable `sqlplus`
    output, and a final `EXIT;`.
  </action>
  <verify>
    <automated>docker compose exec -T oracle sqlplus -s admin/admin@//localhost:1521/FREEPDB1 < scripts/verify_evidence.sql</automated>
  </verify>
  <acceptance_criteria>
    The command exits 0 and prints both result sets against the live Oracle container; the
    business-report query's `GROUP BY` clause is exactly `c.country, TRUNC(o.order_date,
    'MM')` (D-10's literal region-proxy + month-of-order_date spec); no `UPDATE`/`INSERT`/
    `DELETE` statement appears anywhere in the file (read-only evidence only).
  </acceptance_criteria>
  <done>scripts/verify_evidence.sql is committed and reproducibly returns real row-count and business-report evidence (D-09/D-10).</done>
</task>

<task type="auto">
  <name>Task 2: scripts/regenerate_readme_summary.py — real trigger + evidence capture, seeds README's Executive Summary now</name>
  <files>scripts/regenerate_readme_summary.py, README.md</files>
  <read_first>
    scripts/dag_polling.py (Plan 01's `trigger_dag`/`wait_for_task_state`/
    `wait_for_dag_run_result` functions — reuse directly, do not re-derive the trigger/poll
    flow); scripts/verify_evidence.sql (Task 1's own output — mirror its business-report
    query text exactly in this script's Oracle call so the two never silently diverge, per
    this plan's own prohibition); packages/csv-processor/src/csv_processor/load.py
    (`get_connection()` — reuse for the direct `oracledb` business-report query); README.md
    (current content — this task inserts NEW marker-delimited content at the very top,
    before the existing `# Lightweight Airflow CSV->Oracle ETL Platform` heading, and must
    not otherwise alter existing README content).
  </read_first>
  <action>
    Write `scripts/regenerate_readme_summary.py`: for each dataset in `["customers",
    "orders"]`, generate a small fresh fixture via `generator.generate_csv.generate_rows`/
    `write_csv` with a run-unique seed/filename component (e.g. current UTC timestamp) so
    `LOAD-04`'s checksum idempotency never silently returns a STALE prior result on a
    content-identical re-run — this would defeat D-12's "live" requirement; call
    `dag_polling.trigger_dag(dataset, config_path)` BEFORE writing the fixture file, poll
    `dag_polling.wait_for_task_state(..., "wait_for_file", "deferred")`, THEN write the
    fixture (matching Pitfall 4's exact ordering, same as Plan 01's e2e test), then
    `dag_polling.wait_for_dag_run_result(...)` to completion; for the `customers` dataset
    specifically, record the observed deferred-state timestamp and `dag_run_id` as the
    "deferred-wake proof line" (D-11b) — captured from THIS run, not spliced together from a
    different job's run (per 06-RESEARCH.md's Open Question 2 recommendation). After both
    datasets complete, open one `oracledb` connection via `load.get_connection()` and run the
    exact two queries from `scripts/verify_evidence.sql` (row counts + business report,
    `FETCH FIRST 10 ROWS ONLY` on the business report for the README's top-N — D-11c) to get
    structured Python data. Render a Markdown block: dataset row-count table (D-11a), the
    deferred-wake proof line with its timestamp/`dag_run_id` (D-11b), and a top-10 business
    report table (D-11c). Splice it into `README.md` between
    `<!-- EXEC-SUMMARY:START -->`/`<!-- EXEC-SUMMARY:END -->` HTML-comment markers, creating
    them at the top of the file (before the existing `# Lightweight Airflow...` heading) on
    this first run, and replacing only the content between the markers on any subsequent run
    (idempotent regex/string-split on the marker pair, never a naive full-file overwrite). If
    ANY step fails (trigger, poll timeout, Oracle query, file write), the script must exit
    non-zero and must NOT write partial/stale content into README.md — this plan's own
    prohibition against a silent stale/misleading Executive Summary. Run this script for real
    now (against the already-live stack) so README.md is seeded with genuine evidence
    immediately, not left as a placeholder.
  </action>
  <verify>
    <automated>uv run python scripts/regenerate_readme_summary.py && grep -c "EXEC-SUMMARY:START" README.md</automated>
  </verify>
  <acceptance_criteria>
    The script exits 0 against the live stack; `README.md` contains exactly one
    `<!-- EXEC-SUMMARY:START -->`/`<!-- EXEC-SUMMARY:END -->` marker pair with real (non-
    placeholder) row counts, a deferred-wake proof line naming a real timestamp/`dag_run_id`,
    and a business-report table with at least one data row; re-running the script a second
    time still exits 0 and replaces only the content between the markers (the rest of
    README.md is byte-identical outside the marked section).
  </acceptance_criteria>
  <done>README.md's Executive Summary is seeded with real evidence from a live run, regenerable idempotently (D-11).</done>
</task>

<task type="auto">
  <name>Task 3: .github/workflows/readme-summary.yml — push-to-main auto-commit, default GITHUB_TOKEN</name>
  <files>.github/workflows/readme-summary.yml</files>
  <read_first>
    .github/workflows/ci.yml (Plan 03's own oracle-e2e job — mirror its exact `docker
    compose up -d --wait` bring-up steps and first-clone-file recreation sequence verbatim,
    so the two workflows never silently drift apart, per 06-RESEARCH.md Pattern 4's explicit
    "reuses the same docker compose up + trigger pattern as Pattern 3" instruction).
  </read_first>
  <action>
    Write `.github/workflows/readme-summary.yml`: `name: readme-summary`, `on: push:
    branches: [main]`, one job `regenerate-executive-summary` with
    `permissions: contents: write` scoped to ONLY this job (never workflow-level — T-06-03's
    same mitigation applied here, this file's own trust boundary is narrower than ci.yml's
    but the scoping discipline is identical); steps: `actions/checkout@v7.0.1`, the same
    first-clone-file recreation + `docker compose up -d --wait` sequence as Plan 03's
    `oracle-e2e` job, `astral-sh/setup-uv@v10.0.1`, `uv sync --locked`, `uv run python
    scripts/regenerate_readme_summary.py`, then
    `stefanzweifel/git-auto-commit-action@v7.2.0` with `commit_message: "docs(readme):
    regenerate Executive Summary [skip ci]"` and `file_pattern: README.md` — using the
    default `GITHUB_TOKEN` (no PAT/secret configured anywhere in this file; a commit
    authored by the default token structurally cannot re-trigger a `push`-based workflow run,
    per Pitfall 2 — `[skip ci]` is kept in the message anyway as defense-in-depth
    documentation of intent, matching D-13's literal ask, even though it costs nothing extra
    here). A `docker compose down -v` teardown step with `if: always()`.
  </action>
  <verify>
    <automated>python3 -c "import yaml; d = yaml.safe_load(open('.github/workflows/readme-summary.yml')); assert d['on'] == {'push': {'branches': ['main']}}; j = d['jobs']['regenerate-executive-summary']; assert j['permissions'] == {'contents': 'write'}; assert 'permissions' not in d"</automated>
  </verify>
  <acceptance_criteria>
    `.github/workflows/readme-summary.yml` parses as valid YAML; its trigger is exactly
    `push: branches: [main]` (never `pull_request`); `permissions: contents: write` appears
    ONLY inside the `regenerate-executive-summary` job block, never at the workflow's
    top level; no PAT/secret reference (e.g. `secrets.PAT`/`secrets.GH_TOKEN`) appears
    anywhere in the file — `stefanzweifel/git-auto-commit-action@v7.2.0` runs under the
    implicit default `GITHUB_TOKEN`.
  </acceptance_criteria>
  <done>readme-summary.yml automates the Executive Summary regeneration on every merge to main using the default token, never a PAT (D-12/D-13).</done>
  <reversibility rating="costly">Implements D-12 (already user-approved, recorded as "costly" in 06-CONTEXT.md: undoing this means removing a CI write-back step and re-establishing a different update path; also a one-way door on repo hygiene, bot commits in history). Informative only here — no new checkpoint required, the decision was already gated at the discuss-phase stage.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `push: main` (post-merge, trusted code only) -> `regenerate-executive-summary` job | Runs only after a PR's required checks already passed and merged — never runs on untrusted fork PR code |
| `regenerate-executive-summary` job -> repo `contents: write` | The narrowest possible write scope this phase introduces; must never leak to `lint-type-unit`/`oracle-e2e` (Plan 03's PR-triggered jobs, which run on untrusted forks) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-06-07 | Tampering (infinite CI-trigger loop) | `.github/workflows/readme-summary.yml` auto-commit step | medium | mitigate | Default `GITHUB_TOKEN` (never a PAT) for `stefanzweifel/git-auto-commit-action@v7.2.0` — a `GITHUB_TOKEN`-authored commit structurally does not trigger further `push`-based workflow runs (confirmed via the action's own README this phase's research), preventing D-13's feared loop by construction, not just by convention |
| T-06-08 | Elevation of Privilege | `permissions: contents: write` scope | high | mitigate | Scoped exclusively to the `regenerate-executive-summary` job block, never set at workflow level — `readme-summary.yml` has only one job, so there is no risk of an unrelated job inheriting write access within this file |
| T-06-09 | Tampering | `stefanzweifel/git-auto-commit-action` (third-party action) | high | mitigate | Pinned to the exact, live-verified tag `v7.2.0` (GitHub Releases API, this phase's research), never a floating major-version tag |

</threat_model>

<verification>
`docker compose exec -T oracle sqlplus ... < scripts/verify_evidence.sql` returns real evidence.
`uv run python scripts/regenerate_readme_summary.py` exits 0 and seeds README.md's Executive
Summary with real data. `readme-summary.yml` parses as valid YAML with `contents: write` scoped
only to its one job and no PAT anywhere in the file.
</verification>

<success_criteria>
- `scripts/verify_evidence.sql` is a real, committed, reproducible SQL script (D-09).
- README.md's Executive Summary section exists NOW, seeded with real evidence from this plan's
  own live run (D-11), and is regenerable by re-running `scripts/regenerate_readme_summary.py`.
- `.github/workflows/readme-summary.yml` automates that regeneration on every merge to `main`
  using the default `GITHUB_TOKEN`, never a PAT (D-12/D-13, Pitfall 2).
</success_criteria>

<output>
Create `.planning/phases/06-end-to-end-verification-benchmark-ci-docs/06-04-SUMMARY.md` when done
</output>
