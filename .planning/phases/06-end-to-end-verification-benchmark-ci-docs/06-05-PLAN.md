---
phase: 06-end-to-end-verification-benchmark-ci-docs
plan: 05
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03", "06-04"]
files_modified:
  - docs/architecture.md
  - docs/configuration.md
  - docs/csv-engine.md
  - docs/oracle.md
  - docs/development.md
  - README.md
  - Makefile
autonomous: true
requirements: [DOC-01]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Following only README.md and the linked docs/*.md files, a new developer can go from `git clone` to a completed HTTP-triggered ingestion with no undocumented manual steps (DOC-01)."
    - "README.md is a short summary + links (D-16), not a full walkthrough — the Executive Summary (Plan 04's output) stays intact at the very top, untouched, with the summary/links content added below/after it."
    - "docs/development.md documents local dev workflow, architecture/contribution notes, AND CI/troubleshooting with commands copied verbatim from .github/workflows/ci.yml — never paraphrased (D-17, Pitfall 7)."
  artifacts:
    - docs/architecture.md
    - docs/configuration.md
    - docs/csv-engine.md
    - docs/oracle.md
    - docs/development.md
  key_links:
    - "docs/development.md -> .github/workflows/ci.yml (verbatim `run:` command copies, Pitfall 7 — a contributor must be able to copy-paste from the doc and get the identical command CI ran)"
    - "README.md -> docs/*.md (summary + links, D-16 — never duplicates topic-doc content inline)"
    - "Makefile verify-phase6 -> tests/unit, tests/e2e, ruff/mypy, scripts/verify_evidence.sql (the final combined phase-gate target, mirrors verify-phase4/verify-phase5's own 'requires make up first' convention)"
  prohibitions: []
  assumptions:
    - "DOC-01 edge probe (unclassified, deterministic classifier surfaced but did not auto-resolve): no additional edge-case decomposition beyond D-15/D-16/D-17's already-explicit doc-structure spec applies to this requirement — reviewed and not further decomposed."
      status: unresolved
      flagged: true
---

<objective>
Write the five new topic docs (D-15), rewrite README.md as "summary + links" below the already-
seeded Executive Summary (D-16), write `docs/development.md` covering local dev/architecture/
CI-troubleshooting with commands copied verbatim from `.github/workflows/ci.yml` (D-17, Pitfall 7),
and add every new Makefile target (`benchmark`, `lint`, `typecheck`, `verify-evidence`,
`verify-phase6`) this phase introduced, in one place, now that every underlying capability exists.

Purpose: DOC-01 — a new developer must be able to go from `git clone` to a completed HTTP-
triggered ingestion using only README.md and `docs/`, with zero undocumented manual steps.
Output: `docs/architecture.md`, `docs/configuration.md`, `docs/csv-engine.md`, `docs/oracle.md`,
`docs/development.md`, an updated README.md, and the phase's final `Makefile` targets.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-end-to-end-verification-benchmark-ci-docs/06-CONTEXT.md
@.planning/phases/06-end-to-end-verification-benchmark-ci-docs/06-RESEARCH.md
@README.md
@docs/environment.md
@docs/airflow-dag.md
@Makefile
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/architecture.md + docs/configuration.md</name>
  <files>docs/architecture.md, docs/configuration.md</files>
  <read_first>
    CLAUDE.md (the two-tier reference-repo reuse decision — Tier A vendored detection files,
    Tier B algorithm-only reads — belongs in architecture.md's "prior art" framing);
    airflow/dags/csv_ingest.py (the task graph docstring at the top of the file — the
    authoritative source for the DAG's own architecture description); docker-compose.yml
    (the 6-service topology); packages/csv-processor/src/csv_processor/config/models.py
    (`ColumnSpec`/`DatasetConfig`/`OracleTargetSpec`-shaped model tree, `is_safe_identifier`);
    packages/csv-processor/src/csv_processor/config/loader.py (`load_config`'s shallow-merge-
    over-defaults behavior); configs/datasets/customers.json, configs/datasets/orders.json,
    configs/defaults.json (the real, working example configs to reference by example, never
    invented placeholder JSON).
  </read_first>
  <action>
    Write `docs/architecture.md`: a system-level description of the full path (HTTP trigger
    -> Airflow DAG -> `csv_processor.engine.process()` -> Oracle), the component boundary
    between `airflow/dags/` (thin orchestration, zero CSV/Oracle logic of its own) and
    `packages/csv-processor/` (Airflow-agnostic, ENGINE-09), the two-tier reference-repo
    reuse decision (Tier A vendored detection files with the `dataplat` import stripped,
    Tier B algorithm-only reads for pipeline-coupled code), and a plain-text/ASCII component
    diagram of the 6-service `docker-compose.yml` topology. Write `docs/configuration.md`:
    the `config.json` contract shape (`ColumnSpec` fields: `name`/`type`/`nullable`/
    `required`/`format`/`precision`/`scale`; `DatasetConfig`'s top-level `dataset`/
    `file_pattern`/`columns`/`oracle`/`processing` keys), how `defaults.json` shallow-merges
    under a dataset's own config (dataset keys win on collision, top-level only, never
    recursive), and the two real working examples (`configs/datasets/customers.json`,
    `configs/datasets/orders.json`) referenced by their actual field values, never invented
    placeholder JSON. Match `docs/airflow-dag.md`/`docs/environment.md`'s existing heading
    structure and prose voice (both already-established docs in this repo).
  </action>
  <verify>
    <automated>test -f docs/architecture.md && test -f docs/configuration.md && grep -c "config.json" docs/configuration.md</automated>
  </verify>
  <acceptance_criteria>
    Both files exist, non-empty, and reference real file paths/field names from this repo's
    own code (`ColumnSpec`, `DatasetConfig`, the two real dataset config files) rather than
    generic/invented examples; `docs/configuration.md` documents the shallow-merge-over-
    defaults behavior explicitly (a reader must be able to predict what a merged config looks
    like without reading `loader.py` themselves).
  </acceptance_criteria>
  <done>docs/architecture.md and docs/configuration.md exist, matching established repo doc voice and referencing real code/config.</done>
</task>

<task type="auto">
  <name>Task 2: docs/csv-engine.md + docs/oracle.md</name>
  <files>docs/csv-engine.md, docs/oracle.md</files>
  <read_first>
    packages/csv-processor/src/csv_processor/engine.py (`process()`'s full docstring —
    the authoritative 7-step sequence: config re-validation -> file-exists check -> SHA-256
    checksum -> Oracle connection -> idempotency check -> chunked process+load -> record +
    commit; `process_chunks()`'s docstring — structural-short-circuit-before-type-check
    ordering, D-13); packages/csv-processor/src/csv_processor/models.py (`Status`'s 7 closed
    enum values, `ProcessingResult`'s fields); docker/oracle/init/01_ingestion_metadata.sql,
    02_customers.sql, 03_orders.sql, 04_widen_invalid_columns.sql (the real DDL — table/
    column names, `UNIQUE(dataset, checksum)`, `INTERVAL` daily partitioning, and why
    `_invalid` tables' data columns are widened to nullable `VARCHAR2`); packages/csv-processor/src/csv_processor/load.py
    (`insert_rows()`'s `executemany()` bulk pattern, `find_existing_ingestion`/
    `record_ingestion`'s idempotency round trip).
  </read_first>
  <action>
    Write `docs/csv-engine.md`: the detect -> parse -> validate -> normalize -> chunk
    sequence, the structural-short-circuit-before-type-check ordering (a wrong-column-count
    row never reaches `validate.check_row`), the 7 closed `Status` values and what each
    means, `ProcessingResult`'s fields, and the bounded-memory chunking guarantee (ENGINE-07 —
    one chunk in memory at a time, detection runs once per file not once per chunk). Write
    `docs/oracle.md`: the 5-table schema (`<dataset>_valid`/`<dataset>_invalid`/
    `ingestion_metadata`), why `_invalid` tables widen every data column to nullable
    `VARCHAR2` (original malformed values must be storable, never silently coerced), the
    `INTERVAL` daily-partitioning scheme (auto-creates each new day's partition on first
    insert, no manual maintenance job), the `executemany()` bulk-insert mechanism (LOAD-01/
    LOAD-02, never a per-row `INSERT` loop in production code — contrast this explicitly with
    `benchmark/naive_loader.py`'s deliberately-naive comparison code, which exists ONLY to
    demonstrate why `executemany()` is the real path), and the checksum-based idempotency
    round trip (`UNIQUE(dataset, checksum)`, `find_existing_ingestion`/`record_ingestion`,
    LOAD-04's "reprocessing is a safe no-op" guarantee).
  </action>
  <verify>
    <automated>test -f docs/csv-engine.md && test -f docs/oracle.md && grep -c "executemany" docs/oracle.md</automated>
  </verify>
  <acceptance_criteria>
    Both files exist, non-empty; `docs/csv-engine.md` names all 7 `Status` enum values
    verbatim; `docs/oracle.md` explains the idempotency mechanism (`UNIQUE(dataset,
    checksum)`) and the bulk-insert mechanism (`executemany()`), each with a one-sentence
    "why," not just a restatement of the DDL/code.
  </acceptance_criteria>
  <done>docs/csv-engine.md and docs/oracle.md exist, accurately documenting the engine's status contract and Oracle's schema/idempotency/bulk-load mechanisms.</done>
</task>

<task type="auto">
  <name>Task 3: docs/development.md + README.md rewrite (below the Executive Summary) + final Makefile targets</name>
  <files>docs/development.md, README.md, Makefile</files>
  <read_first>
    .github/workflows/ci.yml (Plan 03's own file — copy its `run:` step commands VERBATIM
    into docs/development.md's "reproduce CI locally" section, never paraphrase, per
    Pitfall 7); Makefile (the existing `verify-phase4`/`verify-phase5` "requires `make up`
    first" convention and the file's own closing comment inviting later-phase targets);
    README.md (current content, including the Executive Summary markers Plan 04 already
    inserted at the very top — this task must NOT remove, reorder, or edit anything between
    `<!-- EXEC-SUMMARY:START -->` and `<!-- EXEC-SUMMARY:END -->`, only add new "summary +
    links" content immediately after `<!-- EXEC-SUMMARY:END -->` and before the existing
    `# Lightweight Airflow...` heading, or restructure the existing body without touching the
    marker block); docs/environment.md, docs/airflow-dag.md (existing docs this README links
    into rather than duplicating, per D-16).
  </read_first>
  <action>
    Write `docs/development.md` covering all three of D-17's areas: (a) local dev workflow —
    running `tests/unit/`/`tests/integration/`/`tests/e2e/`, resetting the Oracle container
    (`make reset`), regenerating fixtures (`make generate`/`make fixtures`), running
    `ruff`/`mypy` locally before pushing; (b) architecture/contribution notes — code layout
    (`packages/csv-processor/` vs. `airflow/dags/` vs. `benchmark/` vs. `scripts/`), how to
    add a new dataset (new `configs/datasets/<name>.json` + Oracle DDL + no DAG code changes,
    per DAG-05's zero-branching design), coding conventions matching CLAUDE.md's own style
    (docstrings cite decision IDs, explain why not just what); (c) CI/troubleshooting — copy
    `.github/workflows/ci.yml`'s exact `run:` commands verbatim for both jobs, document the
    Branch Protection manual configuration step Plan 03 flagged (Pitfall 6 — exact repo
    Settings path and the two job names to require), and note the `slim-faststart` Oracle-tag
    fallback for CI disk pressure (Pitfall 5) if ever observed. Rewrite README.md: preserve
    the Executive Summary marker block exactly as-is at the top; below it, keep/expand the
    existing "Getting Started" one-fenced-bash-block-plus-prose style (D-16) and add a
    "Documentation" section linking to every `docs/*.md` file (architecture, configuration,
    csv-engine, oracle, environment, airflow-dag, development, benchmark) with a one-line
    description each — README stays "summary + links," the actual command sequences live in
    the linked docs, never duplicated inline. Add to `Makefile` (update the `.PHONY` line
    too): `benchmark` (`uv run python -m benchmark.run_benchmark --mode bulk --rows 100000`),
    `lint` (`uv run ruff check . && uv run ruff format --check . && uv run mypy .`),
    `verify-evidence` (`docker compose exec -T oracle sqlplus -s
    admin/admin@//localhost:1521/FREEPDB1 < scripts/verify_evidence.sql`), and
    `verify-phase6` (mirrors `verify-phase4`/`verify-phase5`'s exact shape: `uv run pytest
    tests/unit/ -x`, then `uv run pytest tests/e2e/ -x`, then `$(MAKE) lint`, then
    `$(MAKE) verify-evidence` — requires `make up` first, documented in the target's own
    comment).
  </action>
  <verify>
    <automated>test -f docs/development.md && grep -c "EXEC-SUMMARY:START" README.md && grep -c "verify-phase6" Makefile</automated>
  </verify>
  <acceptance_criteria>
    `docs/development.md` exists and its CI-command section is byte-identical to the `run:`
    lines in `.github/workflows/ci.yml` (diff them to confirm, not eyeballed); README.md's
    `<!-- EXEC-SUMMARY:START -->`/`<!-- EXEC-SUMMARY:END -->` marker block is unchanged
    (byte-identical) from before this task; `Makefile` contains `benchmark`, `lint`,
    `verify-evidence`, and `verify-phase6` targets, all listed in `.PHONY`;
    `make verify-phase6` runs to completion against a live `make up` stack (unit suite, e2e
    suite, lint, and evidence verification all exit 0).
  </acceptance_criteria>
  <done>DOC-01 is satisfied: docs/development.md, the rewritten README.md, and the final Makefile targets together let a new developer go from clone to HTTP-triggered ingestion with no undocumented step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Documentation content -> reader's own local setup | Docs must never instruct a reader toward a less-secure configuration than this project already ships (e.g. widening a port bind to `0.0.0.0`, disabling `AIRFLOW__CORE__TEST_CONNECTION`'s default-off posture for a real deployment) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-06-10 | Information Disclosure (documentation-driven misconfiguration) | `docs/*.md`, README.md | low | accept | This phase's docs only ever restate this project's own already-secure, already-documented defaults (127.0.0.1-only port binds per `docs/environment.md`, local-dev-only `admin`/`admin` per INFRA-03) — no new configuration guidance is introduced that could widen the attack surface; accepted as low-risk since these docs are descriptive of existing, already-reviewed behavior, not prescriptive of new settings |

</threat_model>

<verification>
`docs/architecture.md`, `docs/configuration.md`, `docs/csv-engine.md`, `docs/oracle.md`,
`docs/development.md` all exist and are non-empty. README.md's Executive Summary marker block is
untouched. `make verify-phase6` runs to completion against a live stack.
</verification>

<success_criteria>
- All five new topic docs exist, matching this repo's established doc voice/structure.
- README.md is "summary + links" (D-16) with the Executive Summary intact at the top.
- `docs/development.md`'s CI section is byte-identical to `.github/workflows/ci.yml`'s real
  `run:` commands (Pitfall 7).
- `make verify-phase6` exists as the phase's final combined local gate.
</success_criteria>

<output>
Create `.planning/phases/06-end-to-end-verification-benchmark-ci-docs/06-05-SUMMARY.md` when done
</output>
