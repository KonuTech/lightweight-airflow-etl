---
phase: 05-airflow-dag-wiring-deferrable-file-wait
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - airflow/dags/csv_ingest.py
  - airflow/dags/_common/__init__.py
  - airflow/dags/_common/paths.py
  - airflow/dags/_common/reporting.py
  - docker-compose.yml
  - tests/unit/dags/__init__.py
  - tests/unit/dags/conftest.py
  - tests/unit/dags/test_dag_helpers.py
  - tests/unit/dags/test_load_config_helpers.py
  - tests/unit/dags/test_report_result_format.py
autonomous: true
requirements: [DAG-01, DAG-02, DAG-03, DAG-04, DAG-05]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Triggering csv_ingest with valid runtime conf runs load_config_task -> route_after_config -> wait_for_file -> process_csv_task -> load_results_task -> report_result_task in order, calling only csv_processor.config.loader.load_config() and csv_processor.engine.process() -- no CSV/Oracle logic reimplemented in DAG code -- confirmed via a live triggered run for the customers dataset reaching state=='success' (DAG-01)."
    - "A runtime conf with a dataset value outside {customers, orders} (rejected by Param(enum=[...]) at trigger time) or a config_path that resolves outside configs/datasets/ (rejected by resolve_safe_config_path(), including an absolute config_path that would otherwise silently discard the base directory via os.path.join/Path.__truediv__) results in a CONFIGURATION_ERROR-shaped early exit reaching report_result_task's log -- never an unhandled exception or a stuck/failed Airflow task (DAG-02, D-08)."
    - "wait_for_file is declared deferrable=True and, when the target file is absent at trigger time, its task instance reports Airflow state deferred via the REST API taskInstances endpoint -- not merely a class attribute check (DAG-03, D-04)."
    - "report_result_task's log line contains dataset=, file=, status=, total=, valid=, invalid=, and duration= -- proven by _common/reporting.format_summary_log()'s unit test and by the live triggered run's load_results_task XCom content (DAG-04, D-07)."
    - "The csv_ingest DAG file contains no dataset-specific branch (e.g. 'if dataset == customers') -- route_after_config's only conditional keys off config validity, never dataset identity, so the same file structurally supports both datasets by construction (DAG-05, D-01, D-05)."
    - "A retried wait_for_file poke or a retried process_csv_task (e.g. after a worker/triggerer restart) is safe: wait_for_file re-globs with no side effects, and a retried process() call for the same file is a no-op past the first successful insert -- process()'s own checksum-based idempotency (Phase 4 D-01, carried forward as D-11) makes concurrent or repeated runs for the same dataset+file converge on one recorded outcome, with no DAG-level locking needed (DAG-02/DAG-03/DAG-04 concurrency)."
  artifacts:
    - airflow/dags/csv_ingest.py
    - airflow/dags/_common/__init__.py
    - airflow/dags/_common/paths.py
    - airflow/dags/_common/reporting.py
    - tests/unit/dags/test_dag_helpers.py
    - tests/unit/dags/test_load_config_helpers.py
    - tests/unit/dags/test_report_result_format.py
  key_links:
    - "csv_ingest.py's process_csv_task -> csv_processor.engine.process() -- the sole Oracle-writing integration point (D-02, D-12)"
    - "csv_ingest.py's load_config_task -> csv_processor.config.loader.load_config(path, defaults_path=...) -- the sole config-validation integration point (D-08)"
    - "docker-compose.yml's ./configs:/opt/airflow/configs:ro mount -> load_config_task's filesystem read of /opt/airflow/configs/datasets/<dataset>.json inside the container (new gap closed this plan)"
    - "docker-compose.yml's ORACLE_DSN/ORACLE_APP_USER/ORACLE_APP_USER_PASSWORD env vars -> csv_processor.load.oracle_dsn()/oracle_user()/oracle_password(), consumed transitively via process() (new gap closed this plan)"
    - "docker-compose.yml's AIRFLOW_CONN_FS_DEFAULT -> FileSensor's fs_conn_id resolution via FSHook.get_path() (new gap closed this plan)"
    - "wait_for_file's Jinja filepath template -> load_config_task's XCom'd file_pattern via ti.xcom_pull(task_ids='load_config_task')"
  prohibitions:
    - "No DAG task other than process_csv_task may import csv_processor.load or oracledb, call csv_processor.engine.process(), or otherwise open a second Oracle connection / re-attempt an insert within one triggered run (D-02/D-12) -- load_results_task is a pure pass-through."
    - "csv_ingest.py must contain no dataset-specific branch (e.g. 'if dataset == customers') -- config-driven only, per D-01/D-05; route_after_config's branching keys off config validity, never dataset identity."
    - "report_result_task must not call out to Slack/email/any external notification service -- Airflow task logging only, per D-07."
---

<objective>
Build the one, config-driven `csv_ingest` Airflow DAG (D-01) end-to-end for a single concrete
triggered run of the `customers` dataset -- the phase's tracer -- proving `load_config_task`,
the deferrable `wait_for_file` file-wait (D-04), `process_csv_task`'s single call into Phase 4's
`csv_processor.engine.process()` (D-02/D-03/D-12), the config-error early-exit path (D-08), and
`report_result_task`'s logging (D-07) all work for real against the running docker-compose stack.
Also closes two genuine, previously-unconfirmed infrastructure gaps discovered during this
plan's own research pass (in addition to 05-RESEARCH.md's already-documented `ORACLE_DSN` gap):
`configs/` is not mounted into the Airflow containers at all (so `load_config_task` could never
read `configs/datasets/<dataset>.json`), and `fs_default` (the `FileSensor`'s default connection)
is never registered, since `docker-compose.yml`'s `airflow-init` only runs `db migrate`, which
Airflow 3.x's own release notes confirm no longer auto-creates default connections.

Purpose: prove the DAG's whole shape end-to-end on one path before expanding test coverage --
tracer-first decomposition.
Output: `airflow/dags/csv_ingest.py`, `airflow/dags/_common/{paths,reporting}.py`, a fixed
`docker-compose.yml`, and unit tests for the pure-Python helpers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-airflow-dag-wiring-deferrable-file-wait/05-CONTEXT.md
@.planning/phases/05-airflow-dag-wiring-deferrable-file-wait/05-RESEARCH.md
@.planning/phases/05-airflow-dag-wiring-deferrable-file-wait/05-PATTERNS.md
@packages/csv-processor/src/csv_processor/engine.py
@packages/csv-processor/src/csv_processor/models.py
@packages/csv-processor/src/csv_processor/config/loader.py
@packages/csv-processor/src/csv_processor/config/models.py
@packages/csv-processor/src/csv_processor/config/errors.py
@packages/csv-processor/src/csv_processor/load.py
@configs/datasets/customers.json
@configs/datasets/orders.json
@configs/defaults.json
@docker-compose.yml
@docker/airflow/Dockerfile
@Makefile
@scripts/verify_environment.py
@tests/unit/test_config_loader.py
@tests/unit/test_no_airflow_import.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: csv_ingest DAG wired end-to-end, one live triggered run (customers)</name>
  <files>airflow/dags/csv_ingest.py, airflow/dags/_common/__init__.py, airflow/dags/_common/paths.py, airflow/dags/_common/reporting.py, docker-compose.yml</files>
  <read_first>
    packages/csv-processor/src/csv_processor/engine.py (process()'s exact signature, "never
    raises" docstring), packages/csv-processor/src/csv_processor/models.py (Status enum,
    ProcessingResult's exact field list), packages/csv-processor/src/csv_processor/config/loader.py
    (load_config(path, *, defaults_path) -- keyword-only defaults_path, ConfigurationError on
    failure), packages/csv-processor/src/csv_processor/config/models.py (DatasetConfig,
    is_safe_identifier), docker-compose.yml (x-airflow-common's environment/volumes anchors --
    the AIRFLOW_CONN_ORACLE_DEFAULT line's style is the pattern to match), configs/datasets/
    customers.json and configs/datasets/orders.json (real file_pattern values), 05-RESEARCH.md's
    "Code Examples" full DAG skeleton and Pitfalls 1/2/3/4/5/6.
  </read_first>
  <precondition>A local Docker Desktop/WSL2 host capable of running `docker compose` for this
    project's Oracle+Airflow stack is required (already established and proven in Phases 1-4);
    `make up` must succeed before this task's live-trigger verification steps run.</precondition>
  <reversibility rating="reversible">All choices here (task/file names, docker-compose env var
    values, connection registration) are plain config/code changes, cheaply changed later; D-01
    (one DAG, not two) is the only genuinely costly decision in this phase and was already locked
    and reversibility-flagged during /gsd-discuss-phase (05-CONTEXT.md D-01) -- not re-gated
    here.</reversibility>
  <action>
    Build `airflow/dags/_common/paths.py` first (module docstring + Args/Returns/Raises docstring
    style matching `csv_processor/config/loader.py`, zero Airflow import): constants `DATA_ROOT =
    Path("/opt/airflow/data")` (D-05), `CONFIGS_ROOT = Path("/opt/airflow/configs")`,
    `DEFAULTS_PATH = CONFIGS_ROOT / "defaults.json"`, `DATASETS_CONFIG_ROOT = CONFIGS_ROOT /
    "datasets"`, `ALLOWED_DATASETS = frozenset({"customers", "orders"})`; function
    `resolve_matched_file(base_dir: Path, file_pattern: str) -> Path | None` that returns
    `sorted(base_dir.glob(file_pattern))[0]` or `None` if no match (Pitfall 1 -- `FileSensor.poke()`
    only ever returns a bare bool, `process_csv_task` must independently re-glob); function
    `validate_dataset(dataset: str) -> None` raising `ValueError` when `dataset not in
    ALLOWED_DATASETS` (defense-in-depth re-check alongside the DAG's own `Param(enum=[...])`,
    matching this codebase's existing two-layer `is_safe_identifier` convention); function
    `resolve_safe_config_path(config_path: str) -> Path` that FIRST rejects `Path(config_path)
    .is_absolute()` with a `ValueError` (Python's `Path.__truediv__`/`os.path.join` silently
    discards the left operand when the right operand is absolute -- an unguarded join would let an
    absolute `config_path` bypass the allowlist entirely, T-05-01), THEN resolves `(Path("/opt/
    airflow") / config_path).resolve()` and raises `ValueError` unless
    `resolved.is_relative_to(DATASETS_CONFIG_ROOT.resolve())`.

    Build `airflow/dags/_common/reporting.py` (zero Airflow import): function
    `format_summary_log(result: dict) -> str` returning exactly `f"dataset={result['dataset']}
    file={result['file_name']} status={result['status']} total={result['total_rows']}
    valid={result['valid_rows']} invalid={result['invalid_rows']}
    duration={result['duration_seconds']:.2f}s"` (DAG-04's field list, matches
    `ProcessingResult`'s exact field names from `models.py`).

    Build `airflow/dags/csv_ingest.py`. Imports: `from airflow.sdk import Param, dag,
    get_current_context, task` (Airflow 3's stable Task SDK surface, never
    `airflow.models`/`airflow.decorators`), `from airflow.providers.standard.sensors.filesystem
    import FileSensor`, `from csv_processor.config.loader import load_config`, `from
    csv_processor.config.errors import ConfigurationError`, `from csv_processor.config.models
    import DatasetConfig`, `from csv_processor.engine import process`, `from csv_processor.models
    import ProcessingResult, Status`, plus `from airflow.dags._common import paths, reporting` --
    note `airflow/dags/_common` is imported as a plain top-level package relative to the DAG
    folder (Airflow adds `dag_folder` to `sys.path`), so use `from _common import paths,
    reporting`, not an `airflow.dags._common` dotted path.

    `@dag(dag_id="csv_ingest", schedule=None, catchup=False, params={"dataset": Param("customers",
    type="string", enum=["customers", "orders"]), "config_path": Param(
    "configs/datasets/customers.json", type="string")})` decorating a `csv_ingest()` function.

    `load_config_task` (`@task`, no args, returns `dict`): read `ctx =
    get_current_context()`, `dataset = ctx["params"]["dataset"]`, `config_path =
    ctx["params"]["config_path"]`. Wrap in try/except: call `paths.validate_dataset(dataset)`,
    `resolved_path = paths.resolve_safe_config_path(config_path)`, then `config =
    load_config(resolved_path, defaults_path=paths.DEFAULTS_PATH)` (D-08's exact call shape,
    Pitfall 4's keyword-only `defaults_path`). On success return
    `config.model_dump(mode="json")`. On `(ValueError, ConfigurationError)`, construct and return a
    `ProcessingResult`-shaped dict with `status=Status.CONFIGURATION_ERROR.value`, `dataset=dataset`,
    `file_name=""`, `total_rows=0`, `valid_rows=0`, `invalid_rows=0`, `duration_seconds=0.0`,
    `checksum=None` -- never let the exception propagate (D-08's "CONFIGURATION_ERROR-shaped early
    exit", consistent with D-03's "domain failures don't fail the task").

    `route_after_config` (`@task.branch`, one arg `config_dict: dict`, returns `str`): return
    `"report_result_task"` if `config_dict.get("status") == Status.CONFIGURATION_ERROR.value`,
    else return `"wait_for_file"`. This is the one, deliberate, non-dataset-specific branch in the
    DAG -- it must never be confused with dataset-specific routing (D-05's prohibition is about
    branching on WHICH dataset, not about this error-vs-success control-flow decision).

    `wait_for_file = FileSensor(task_id="wait_for_file", fs_conn_id="fs_default", filepath=
    "/opt/airflow/data/{{ params.dataset }}/{{ ti.xcom_pull(task_ids='load_config_task')
    ['file_pattern'] }}", deferrable=True, poke_interval=10, timeout=3600)` (D-04/D-06 -- the
    globbed `file_pattern` value itself (`customers_*.csv*`/`orders_*.csv*`) already matches
    compressed variants per D-09 (= Phase 3 D-31), so this glob needs no `.gz`/`.zip`
    special-casing here. `filepath` uses an absolute
    path so the Jinja expression is correct regardless of `fs_default`'s configured base path;
    `FileSensor.path` is `os.path.join(FSHook(fs_conn_id).get_path(), filepath)`, and
    `os.path.join` always returns the second argument verbatim when it is absolute, so this works
    whether `fs_default`'s `extra.path` is `"/"`, empty, or anything else). Contingency: if the
    live-trigger verify below shows `ti.xcom_pull(...)` does not render inside this Jinja
    `filepath` string (05-RESEARCH.md Assumption A3), fall back to declaring `file_pattern` as its
    own explicit third `params` field on the `@dag` decorator and reference it as `{{
    params.file_pattern }}` instead.

    `process_csv_task` (`@task`, one arg `config_dict: dict`, returns `dict`): `ctx =
    get_current_context()`, `dataset = ctx["params"]["dataset"]`, `config =
    DatasetConfig.model_validate(config_dict)`, `base_dir = paths.DATA_ROOT / dataset`, `matched =
    paths.resolve_matched_file(base_dir, config.file_pattern)`, `file_path = matched if matched is
    not None else base_dir / "__no_match_found__"` (Pitfall 1's fix -- never read
    `wait_for_file.output`, and reuse `process()`'s own `FILE_NOT_FOUND` status handling for the
    astronomically-unlikely race where the sensor's poke succeeded but the file vanished before
    this task runs, rather than inventing a new error shape). `result: ProcessingResult =
    process(file_path, config)` -- call exactly once, with NO try/except around it that
    re-raises on any `Status` member (D-03/Pitfall 5 -- `process()` never raises); `result
    .file_name` is already the basename only (D-10 = Phase 3 D-08, set inside `process()` itself
    via `file_path.name`), matching what `resolve_matched_file`'s glob resolved -- no adapter
    needed here. Return
    `result.model_dump(mode="json")`.

    `load_results_task` (`@task`, one arg `result_dict: dict`, returns `dict`): `return
    result_dict` verbatim -- D-02's thin pass-through, never imports `csv_processor.load` or
    `oracledb`, never calls `process()` again.

    `report_result_task` (`@task(trigger_rule="none_failed_min_one_success")`, no args, returns
    `None`): `ctx = get_current_context()`, `ti = ctx["ti"]`, `outcome =
    ti.xcom_pull(task_ids="load_results_task") or ti.xcom_pull(task_ids="load_config_task")` (on
    the success path `load_results_task`'s dict is truthy and wins; on the config-error path
    `load_results_task` was skipped so its XCom is `None` and `load_config_task`'s
    CONFIGURATION_ERROR-shaped dict is used instead -- both shapes share the exact field set
    `format_summary_log` expects). `logging.getLogger("airflow.task").info(
    reporting.format_summary_log(outcome))` (D-07 -- logs only, no Slack/email).

    Wiring inside `csv_ingest()`: `config_dict = load_config_task()`; `route =
    route_after_config(config_dict)`; `result_dict = process_csv_task(config_dict)`;
    `final_result_dict = load_results_task(result_dict)`; `report = report_result_task()`; then
    explicit edges `route >> wait_for_file >> result_dict`, `route >> report`, `final_result_dict
    >> report`. Call `csv_ingest()` at module level.

    Fix `docker-compose.yml`'s `x-airflow-common.environment` (`&airflow-common-env` anchor),
    adding these four lines immediately after the existing `AIRFLOW_CONN_ORACLE_DEFAULT` line and
    before `AIRFLOW__CORE__TEST_CONNECTION` (same quoting/interpolation style as the existing
    block): `ORACLE_DSN: "oracle:1521/FREEPDB1"` (Pattern 4 -- the container-internal Oracle EZ
    Connect string; `csv_processor.load.oracle_dsn()` defaults to `localhost:1521/FREEPDB1`, which
    does not resolve inside the Airflow containers), `ORACLE_APP_USER:
    "${ORACLE_APP_USER:-admin}"`, `ORACLE_APP_USER_PASSWORD: "${ORACLE_APP_USER_PASSWORD:-admin}"`,
    and `AIRFLOW_CONN_FS_DEFAULT: "fs://"` (this plan's own newly-discovered gap: `airflow-init`
    only runs `db migrate`, and Airflow's own release notes confirm default connections
    (including `fs_default`) are no longer auto-created during migration -- `FileSensor`'s
    `FSHook(fs_conn_id="fs_default")` raises `AirflowNotFoundException` if no such connection
    exists at all, regardless of `filepath` being absolute). Also add `- ./configs:/opt/airflow/
    configs:ro` to `x-airflow-common.volumes`, alongside the existing `./airflow/dags:/opt/
    airflow/dags` and `./data:/opt/airflow/data` lines (this plan's own newly-discovered gap:
    `configs/` was never mounted or copied into the Airflow image at all, so `load_config_task`
    could never read `configs/datasets/<dataset>.json` without this).
  </action>
  <verify>
    <automated>
docker compose up -d --wait
uv run python generator/generate_csv.py --dataset customers

docker compose exec -T airflow-scheduler python -c "
from airflow.models import DagBag
b = DagBag(dag_folder='/opt/airflow/dags')
assert not b.import_errors, b.import_errors
dag = b.dags['csv_ingest']
required = {'load_config_task','wait_for_file','process_csv_task','load_results_task','report_result_task'}
assert required.issubset(set(dag.task_ids)), dag.task_ids
assert dag.get_task('wait_for_file').deferrable is True
print('DAGBAG_OK')
"

JWT_TOKEN=$(curl -s -X POST "http://localhost:8080/auth/token" \
  -H "Content-Type: application/json" \
  -d '{"username": "admin", "password": "admin"}' | jq -r '.access_token')

RUN_ID=$(curl -s -X POST "http://localhost:8080/api/v2/dags/csv_ingest/dagRuns" \
  -H "Authorization: Bearer ${JWT_TOKEN}" -H "Content-Type: application/json" \
  -d '{"conf": {"dataset": "customers", "config_path": "configs/datasets/customers.json"}}' \
  | jq -r '.dag_run_id')

RESULT=$(curl -s --max-time 120 -H "Authorization: Bearer ${JWT_TOKEN}" \
  -H "Accept: application/x-ndjson" \
  "http://localhost:8080/api/v2/dags/csv_ingest/dagRuns/${RUN_ID}/wait?result=load_results_task" \
  | tail -1)

echo "$RESULT" | jq -e '.state == "success"'
echo "$RESULT" | jq -e '.results.load_results_task.status == "SUCCESS" or .results.load_results_task.status == "SUCCESS_WITH_INVALID_ROWS"'
echo "$RESULT" | jq -e '.results.load_results_task.dataset == "customers"'
    </automated>
  </verify>
  <done>
    `csv_ingest.py` parses with zero import errors, exposes the 5 required task_ids (plus
    `route_after_config`), `wait_for_file.deferrable is True`; a live triggered run for
    `customers` reaches `state == "success"` with `load_results_task`'s XCom showing
    `status` of `SUCCESS` or `SUCCESS_WITH_INVALID_ROWS`; docker-compose.yml carries the four new
    env vars and the `configs/` mount.
  </done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for the pure-Python _common helpers (DAG-02/DAG-04/DAG-05)</name>
  <files>tests/unit/dags/__init__.py, tests/unit/dags/conftest.py, tests/unit/dags/test_dag_helpers.py, tests/unit/dags/test_load_config_helpers.py, tests/unit/dags/test_report_result_format.py</files>
  <read_first>
    airflow/dags/_common/paths.py and airflow/dags/_common/reporting.py (Task 1's own output --
    exact function signatures), tests/unit/test_config_loader.py (this project's established
    pytest style: plain functions, module-level `_CONFIGS_DIR = Path(__file__).resolve()
    .parent.parent.parent / "configs"` constant -- one `.parent` deeper here since this file lives
    under `tests/unit/dags/`), pyproject.toml's `[tool.pytest.ini_options]` (`pythonpath =
    ["."]`, `testpaths = ["tests"]` -- no new pytest config needed, `tests/unit/dags/` is
    auto-discovered).
  </read_first>
  <action>
    This task tests Task 1's already-implemented, already-passing behavior -- a single `test(...)`
    commit with no paired `feat(...)`, matching this project's established carve-out (e.g.
    02-02-PLAN.md's `test_config_models.py`) for proving already-correct code, not a TDD
    RED/GREEN cycle. `tests/unit/dags/__init__.py` empty. `tests/unit/dags/conftest.py`: module
    constant `_REPO_ROOT = Path(__file__).resolve().parent.parent.parent.parent`, `_CONFIGS_DIR =
    _REPO_ROOT / "configs"`, `_DEFAULTS_PATH = _CONFIGS_DIR / "defaults.json"`; a
    `dataset_configs()` pytest fixture returning `{"customers": load_config(_CONFIGS_DIR /
    "datasets" / "customers.json", defaults_path=_DEFAULTS_PATH), "orders": load_config(
    _CONFIGS_DIR / "datasets" / "orders.json", defaults_path=_DEFAULTS_PATH)}` for reuse across
    the three test files below.

    `test_dag_helpers.py` (imports `from _common.paths import resolve_matched_file` -- add
    `sys.path.insert(0, str(_REPO_ROOT / "airflow" / "dags"))` at module top since `_common` is
    only importable from within the DAG folder at Airflow runtime, not via the project's normal
    `pythonpath`): `test_resolve_matched_file_returns_sorted_first_match(tmp_path)` -- create
    `customers_20260101.csv` and `customers_20260201.csv` in `tmp_path`, assert
    `resolve_matched_file(tmp_path, "customers_*.csv*")` returns the path ending in
    `customers_20260101.csv` (sorted-first). `test_resolve_matched_file_returns_none_when_no_match
    (tmp_path)` -- empty `tmp_path`, assert `resolve_matched_file(tmp_path, "customers_*.csv*") is
    None`. `test_resolve_matched_file_works_for_both_dataset_patterns(tmp_path,
    dataset_configs)` parametrized over `("customers", "customers_20990101.csv")` and
    `("orders", "orders_20990101.csv.gz")` -- write the matching filename into `tmp_path`, assert
    `resolve_matched_file(tmp_path, dataset_configs[dataset].file_pattern)` finds it, proving
    DAG-05's "same helper, both datasets, zero branching."

    `test_load_config_helpers.py` (imports `validate_dataset`, `resolve_safe_config_path` from
    `_common.paths`): `test_validate_dataset_accepts_known_datasets()` -- `validate_dataset(
    "customers")` and `validate_dataset("orders")` do not raise.
    `test_validate_dataset_rejects_unknown_dataset()` -- `pytest.raises(ValueError)` around
    `validate_dataset("../../etc")`. `test_resolve_safe_config_path_accepts_real_dataset_configs()`
    parametrized over `"configs/datasets/customers.json"`/`"configs/datasets/orders.json"` --
    result's `.name` equals the expected filename and the result `.is_relative_to(_CONFIGS_DIR /
    "datasets")` (adjusted for the `/opt/airflow` prefix the function assumes -- assert on
    `result.parts[-3:]` == `("configs", "datasets", "<name>.json")` instead of a literal
    `/opt/airflow`-prefixed equality, since this test runs on the host, not inside the container).
    `test_resolve_safe_config_path_rejects_traversal()` -- `pytest.raises(ValueError)` around
    `resolve_safe_config_path("../../etc/passwd")`.
    `test_resolve_safe_config_path_rejects_absolute_paths()` -- `pytest.raises(ValueError)` around
    `resolve_safe_config_path("/etc/passwd")` (proves the `Path.__truediv__`
    absolute-path-discards-base-directory bypass is closed -- T-05-01's mitigation, the single
    most important negative case in this file).

    `test_report_result_format.py` (imports `format_summary_log` from `_common.reporting`):
    `test_format_summary_log_contains_all_required_fields()` -- build a dict `{"status":
    "SUCCESS", "dataset": "customers", "file_name": "customers_20260829.csv", "total_rows": 10,
    "valid_rows": 9, "invalid_rows": 1, "duration_seconds": 1.234, "checksum": "abc"}`, assert the
    returned string contains each of `"dataset=customers"`, `"file=customers_20260829.csv"`,
    `"status=SUCCESS"`, `"total=10"`, `"valid=9"`, `"invalid=1"`, and `"duration=1.23s"` (2
    decimal places, DAG-04's exact field list).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/dags/ -x -v</automated>
  </verify>
  <done>All 3 new test files pass; `resolve_safe_config_path`'s absolute-path and traversal
  rejection cases are both explicitly asserted; both dataset configs are proven through
  `resolve_matched_file` without any dataset-specific branch in the test or the helper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| HTTP caller -> Airflow REST API (`POST /api/v2/dags/csv_ingest/dagRuns`) | Untrusted HTTP input: `conf.dataset`, `conf.config_path` -- anyone holding the local dev credential can supply arbitrary values |
| Airflow DAG task -> container filesystem | `config_path`/`dataset`-derived paths touch `/opt/airflow/configs/` and `/opt/airflow/data/` inside the Airflow containers |
| Airflow DAG task -> Oracle (via `csv_processor.engine.process()`) | Already covered by Phase 4's own threat model; this phase adds exactly one new caller (`process_csv_task`), no new Oracle-facing surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | `load_config_task` (`config_path` conf value) | high | mitigate | `resolve_safe_config_path()` rejects an absolute `config_path` before joining (closes the `Path.__truediv__`/`os.path.join` silent-base-discard bypass) and rejects any resolved path outside `configs/datasets/`; checked before `load_config()` is ever called (D-08) |
| T-05-02 | Tampering | `load_config_task` / `wait_for_file` (`dataset` conf value) | high | mitigate | `Param(dataset, enum=["customers","orders"])` rejects an unlisted value at trigger time via Airflow's JSON-Schema validation on `conf`; `_common/paths.validate_dataset()` re-checks the same allowlist inside the task body, defense-in-depth matching this codebase's existing `is_safe_identifier` two-layer convention |
| T-05-03 | Information Disclosure | `report_result_task` log line | low | accept | Airflow task logs are visible only to the single local `admin` user (INFRA-03); no external log shipping/metrics/tracing in this project's explicit scope (PROJECT.md Out of Scope) |
| T-05-04 | Tampering | `load_results_task` (Oracle write-path integrity) | high | mitigate | `load_results_task` never imports `csv_processor.load`/`oracledb` and never calls `process()` -- pure XCom pass-through (D-02/D-12); only `process_csv_task` touches Oracle, exactly once per run |
</threat_model>

<verification>
`uv run pytest tests/unit/dags/ -x` passes; the `docker compose exec` DagBag structure check
passes; a live triggered run for `customers` completes with `state == "success"` and a
`SUCCESS`/`SUCCESS_WITH_INVALID_ROWS` status visible in `load_results_task`'s XCom.
</verification>

<success_criteria>
The `csv_ingest` DAG exists, parses cleanly, and has been proven end-to-end for one real
triggered run against the actual running stack -- no CSV/Oracle logic reimplemented, config-error
input handled cleanly, deferrable file-wait declared correctly, and the pure-Python helper layer
(`_common/paths.py`, `_common/reporting.py`) is fully unit-tested including the security-relevant
negative cases (path traversal, absolute-path bypass, unknown dataset).
</success_criteria>

<output>
Create `.planning/phases/05-airflow-dag-wiring-deferrable-file-wait/05-01-SUMMARY.md` when done
</output>
