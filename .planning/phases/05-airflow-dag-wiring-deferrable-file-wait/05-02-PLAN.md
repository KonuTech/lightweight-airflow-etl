---
phase: 05-airflow-dag-wiring-deferrable-file-wait
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - Makefile
  - scripts/trigger_dag.sh
  - docs/airflow-dag.md
autonomous: true
requirements: [DAG-03, DAG-05, DAG-04]

estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "wait_for_file's task instance reports Airflow state deferred (observed live via GET /api/v2/dags/csv_ingest/dagRuns/{run_id}/taskInstances/wait_for_file), not merely deferrable=True on the operator definition, when the expected file is genuinely absent at trigger time (DAG-03, D-04)."
    - "The same airflow/dags/csv_ingest.py file, unmodified since Plan 05-01, produces a SUCCESS/SUCCESS_WITH_INVALID_ROWS run for the orders dataset purely by differing runtime conf -- proving DAG-05 for the second dataset with genuine live evidence, not just unit-level parametrization (DAG-05, D-01)."
    - "scripts/trigger_dag.sh reuses the exact /auth/token -> Authorization: Bearer flow already proven in scripts/verify_environment.py -- no new auth mechanism invented (matches RESEARCH.md Pattern 5/Don't Hand-Roll)."
  artifacts:
    - Makefile
    - scripts/trigger_dag.sh
    - docs/airflow-dag.md
  key_links:
    - "scripts/trigger_dag.sh's auth flow -> scripts/verify_environment.py's AIRFLOW_AUTH_TOKEN_URL/AIRFLOW_USER/AIRFLOW_PASSWORD constants (same values, same endpoint, reused not reinvented)"
    - "Makefile's verify-phase5 target -> uv run pytest tests/unit/dags/ (Plan 05-01 Task 2) + the docker compose exec DagBag structure check (Plan 05-01 Task 1), following the established verify-phaseN convention"
  prohibitions:
    - "trigger_dag.sh must not implement its own JWT-issuing or bypass Airflow's /auth/token endpoint -- Airflow's own SimpleAuthManager is the only auth mechanism this project uses (REQUIREMENTS.md Out of Scope: no custom FastAPI wrapper)."
---

<objective>
Prove DAG-03 (deferred file-wait state, observed live against the real triggerer) and DAG-05
(identical DAG, second dataset, zero code changes) with genuine live evidence against the running
stack -- not just unit-level assertions -- and leave behind a reusable trigger script + Makefile
gate + short doc for Phase 6's benefit.

Purpose: close out this phase's remaining requirement coverage with real, observed proof rather
than structural inference alone.
Output: `Makefile`'s `verify-phase5` target, `scripts/trigger_dag.sh`, `docs/airflow-dag.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-airflow-dag-wiring-deferrable-file-wait/05-CONTEXT.md
@.planning/phases/05-airflow-dag-wiring-deferrable-file-wait/05-RESEARCH.md
@.planning/phases/05-airflow-dag-wiring-deferrable-file-wait/05-01-SUMMARY.md
@airflow/dags/csv_ingest.py
@scripts/verify_environment.py
@Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: trigger_dag.sh + Makefile verify-phase5 target</name>
  <files>scripts/trigger_dag.sh, Makefile</files>
  <read_first>
    scripts/verify_environment.py (the exact `/auth/token` auth flow -- `AIRFLOW_AUTH_TOKEN_URL`,
    `AIRFLOW_USER`, `AIRFLOW_PASSWORD` constants and the payload shape to reuse verbatim), Makefile
    (verify-phase4's exact target shape and its own comment: "requires `make up` first" -- follow
    the same convention, do not invoke `make up` from inside `verify-phase5` itself),
    airflow/dags/csv_ingest.py (Plan 05-01's actual task names -- `route_after_config` must be
    included in the DagBag structure check's asserted subset the same way Plan 05-01 Task 1 wrote
    it).
  </read_first>
  <action>
    Create `scripts/trigger_dag.sh`: a bash script accepting two positional args `$1` (dataset,
    e.g. `customers`) and `$2` (config_path, e.g. `configs/datasets/customers.json`), with
    `AIRFLOW_BASE_URL` defaulting to `http://localhost:8080` (overridable via env var). Reuses
    the exact auth flow from `scripts/verify_environment.py`: `POST ${AIRFLOW_BASE_URL}/auth/
    token` with `{"username": "admin", "password": "admin"}`, extract `.access_token` via `jq`.
    `POST ${AIRFLOW_BASE_URL}/api/v2/dags/csv_ingest/dagRuns` with `Authorization: Bearer
    <token>` and body `{"conf": {"dataset": "$1", "config_path": "$2"}}`, extract `.dag_run_id`.
    Print the `dag_run_id` to stdout on its own line (so a caller can capture it via
    `$(scripts/trigger_dag.sh ...)`), and print the auth/trigger request URLs to stderr for
    visibility. `set -euo pipefail` at the top. Make it executable (`chmod +x`).

    Add a `verify-phase5` target to `Makefile`, placed sequentially after `verify-phase4`,
    mirroring its exact two-line shape (full unit suite, then the phase-specific extra check),
    with a comment matching the file's existing convention ("Phase 5 needs a running Airflow
    container to structurally validate the DAG -- requires `make up` first, same as
    verify-phase4"): first line `uv run pytest tests/unit/ -x`, second line the `docker compose
    exec -T airflow-scheduler python -c "..."` DagBag structure check from Plan 05-01 Task 1's own
    `<verify>` (asserting the 5 required task_ids are a subset of `dag.task_ids`, plus
    `wait_for_file.deferrable is True`), reproduced verbatim so `make verify-phase5` is a single,
    self-contained command. Update the `.PHONY` line to include `verify-phase5`.
  </action>
  <verify>
    <automated>test -x scripts/trigger_dag.sh && grep -q "verify-phase5" Makefile && make -n verify-phase5</automated>
  </verify>
  <done>`scripts/trigger_dag.sh` is executable, reuses `scripts/verify_environment.py`'s exact
  auth constants/flow, and prints a `dag_run_id`; `Makefile` has a `verify-phase5` target listed
  in `.PHONY` that runs the full unit suite plus the DagBag structure check.</done>
</task>

<task type="auto">
  <name>Task 2: Live proof of DAG-03 (deferred state) and DAG-05 (orders dataset) + docs</name>
  <files>docs/airflow-dag.md</files>
  <read_first>
    scripts/trigger_dag.sh (Task 1's own output -- exact invocation shape), configs/datasets/
    orders.json (real file_pattern `orders_*.csv*`), generator/generate_csv.py (`--dataset`
    flag, writes to `./data/<dataset>/<dataset>_<YYYYMMDD>.csv`).
  </read_first>
  <precondition>The docker-compose stack from Plan 05-01 must already be running (`make up`) with
    `csv_ingest.py` already picked up by the dag-processor; if `POST dagRuns` 404s because the DAG
    hasn't been parsed yet, wait for the dag-processor's next scan interval (or `docker compose
    restart airflow-scheduler airflow-dag-processor`) and retry. `./data/orders/` must not already
    contain a file matching `orders_*.csv*` when this task's first trigger runs (fresh
    environment; if one exists from a prior manual test, remove it first) so the deferred-state
    observation below is genuine, not a same-poke coincidence.</precondition>
  <action>
    Trigger the DAG for the `orders` dataset BEFORE any `orders` fixture file exists: run
    `RUN_ID=$(scripts/trigger_dag.sh orders configs/datasets/orders.json)`. Poll `GET
    http://localhost:8080/api/v2/dags/csv_ingest/dagRuns/${RUN_ID}/taskInstances/wait_for_file`
    (with the same `Authorization: Bearer` token) every 3 seconds, up to 30 seconds, until the
    response's `.state` field equals `"deferred"` -- this is DAG-03's live proof (the sensor is
    triggerer-managed, not a worker-slot poll loop). Once observed, run `uv run python
    generator/generate_csv.py --dataset orders` to create the file, then call `GET
    http://localhost:8080/api/v2/dags/csv_ingest/dagRuns/${RUN_ID}/wait?result=load_results_task`
    (same `Accept: application/x-ndjson` header as Plan 05-01 Task 1's verify) and confirm the
    final line's `.state == "success"` and `.results.load_results_task.dataset == "orders"` with
    `.results.load_results_task.status` being `SUCCESS` or `SUCCESS_WITH_INVALID_ROWS` -- this is
    DAG-05's live proof for the second dataset, using the identical, unmodified `csv_ingest.py`
    from Plan 05-01.

    Write `docs/airflow-dag.md` documenting: the DAG's task graph (`load_config_task ->
    route_after_config -> wait_for_file -> process_csv_task -> load_results_task ->
    report_result_task`, noting `route_after_config`'s config-error early-exit branch is the only
    conditional in the graph and never keys off dataset identity, D-01/D-05); how to trigger it
    (`scripts/trigger_dag.sh <dataset> <config_path>`, reusing `scripts/verify_environment.py`'s
    auth flow); the exact curl commands used above for the deferred-state and orders-dataset
    proofs (for reproducibility); and a short note that full HTTP-to-Oracle-rows end-to-end
    automated testing is Phase 6's job (TEST-03), this document only records this phase's own
    manual/live verification evidence.
  </action>
  <verify>
    <automated>test -f docs/airflow-dag.md && grep -q "deferred" docs/airflow-dag.md && grep -q "orders" docs/airflow-dag.md</automated>
  </verify>
  <done>A live-triggered `orders` DAG run's `wait_for_file` task instance was observed in
  `deferred` state before the file existed, and the same run later completed with
  `state == "success"` and a `SUCCESS`/`SUCCESS_WITH_INVALID_ROWS` status for `orders` -- both
  facts, plus the reproducible commands, are recorded in `docs/airflow-dag.md`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Developer shell -> Airflow REST API (via `scripts/trigger_dag.sh`) | Same trust boundary as Plan 05-01's T-05-01/T-05-02 -- this plan only adds a convenience wrapper around the same, already-mitigated trigger call, no new input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Tampering | `scripts/trigger_dag.sh` (local dev credential embedded as literal `admin`/`admin`, matching `scripts/verify_environment.py`'s existing convention) | low | accept | Local-dev-only scope (INFRA-03); identical credential-handling pattern already accepted in `scripts/verify_environment.py`, not a new risk this plan introduces |
</threat_model>

<verification>
`scripts/trigger_dag.sh` is executable and reuses the proven auth flow; `make -n verify-phase5`
prints the two-step target without error; a live `orders` DAG run shows `wait_for_file` in
`deferred` state before the file exists and completes successfully once it does;
`docs/airflow-dag.md` records both proofs.
</verification>

<success_criteria>
DAG-03 and DAG-05 are proven with real, observed evidence against the running stack (not just
structural/unit-level inference), and a reusable trigger script plus phase-gate Makefile target
exist for ongoing use and for Phase 6's benefit.
</success_criteria>

<output>
Create `.planning/phases/05-airflow-dag-wiring-deferrable-file-wait/05-02-SUMMARY.md` when done
</output>
