---
phase: 02-config-contract-csv-generator
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - tests/fixtures/corpus.yaml
  - tests/fixtures/CORPUS.sha256
autonomous: true
requirements: [GEN-01]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "make fixtures && make fixtures-verify still exits 0 with zero mismatches after structural, type/nullability, and byte-level-hard fixtures are added — the digest oracle covers all fixtures authored so far (8 dialect/encoding + 8 structural + 6 type/nullability + 5 byte-level-hard = 27 fixtures)."
    - "The type/nullability fixture for an over-scale decimal (18_invalid_decimal_too_many_places) encodes an amount-shaped value with more decimal places than orders.json's configured scale=2, and 22_empty_nullable_field_should_pass encodes an empty value for a nullable (not non-nullable) column — both documented in corpus.yaml's expect: block as free-text prose, never a structured error_code enum (D-16d)."
    - "22_empty_nullable_field_should_pass is explicitly documented in its expect: block as a fixture that SHOULD validate cleanly (a positive/should-pass case), not an error case — distinguishing it from every other fixture in this category."
  prohibitions: []
  artifacts:
    - tests/fixtures/corpus.yaml (extended)
    - tests/fixtures/CORPUS.sha256 (extended)
  key_links:
    - "tests/fixtures/corpus.yaml's structural/type-nullability/byte-level-hard categories -> the same tools/corpus/generators.py 'tabular'/'literal' kinds Plan 03 already built (no new generator kind expected)"
---

<objective>
Scale the fixture corpus (Plan 03's proven mechanism) across three more categories: structural
(8 fixtures — malformed column shape), type/nullability (6 fixtures — this project's own
structural/type/nullability-only validation vocabulary, no reference-repo file-for-file match),
and byte-level-hard (5 fixtures — RFC-4180 edge cases). This plan authors content only; it does
not expect to need new `tools/corpus/generators.py` generator kinds beyond the `tabular`/`literal`
kinds Plan 03 already implemented (the `wrapper` kind for compression is Plan 05's job).

Purpose: bring the corpus to 27 of its ~30 planned fixtures, covering every category except
large/compressed (Plan 05), so Phase 3's future detection-module tests have real malformed-CSV
byte fixtures across structural, type, and byte-level-hard failure classes to run against.
Output: `tests/fixtures/corpus.yaml` extended to 27 entries; `tests/fixtures/CORPUS.sha256`
regenerated and committed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-config-contract-csv-generator/02-CONTEXT.md
@.planning/phases/02-config-contract-csv-generator/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Structural fixture category (8 fixtures)</name>
  <files>tests/fixtures/corpus.yaml, tests/fixtures/CORPUS.sha256</files>
  <read_first>
    tests/fixtures/corpus.yaml (Plan 03's 8 dialect/encoding entries — mirror the same YAML shape),
    tools/corpus/manifest.py, tools/corpus/generators.py,
    .planning/phases/02-config-contract-csv-generator/02-RESEARCH.md (section "Recommended scoped
    fixture category list", row "Structural")
  </read_first>
  <action>
    Author exactly 8 fixtures into `tests/fixtures/corpus.yaml` under category `structural`:
    `09_missing_column` (tabular, header omits one column a real dataset schema declares —
    `expect`: "header is missing a schema-declared column"); `10_extra_unexpected_column` (tabular,
    header has one column not in any dataset schema); `11_duplicate_column_name` (tabular, header
    repeats a column name); `12_wrong_column_count_row` (tabular, one data row has fewer fields
    than the header — a ragged row); `13_empty_file` (literal, zero bytes); `14_header_only_no_rows`
    (tabular, header row only, zero data rows — `expect`: "structurally valid, zero data rows, not
    an error case"); `15_no_header_row` (literal, raw data rows with no header line at all, when a
    header is expected); `16_ragged_rows_and_blank_lines` (literal, a mix of short/long rows plus
    blank lines interspersed, no trailing newline). Run `make fixtures && make fixtures-verify` to
    materialize and prove the round-trip.
  </action>
  <verify>
    <automated>make fixtures && make fixtures-verify</automated>
  </verify>
  <acceptance_criteria>
    - `make fixtures-verify` exits 0.
    - The manifest now declares 16 total fixtures (8 dialect/encoding + 8 structural); `wc -l tests/fixtures/CORPUS.sha256` reports 16 lines.
  </acceptance_criteria>
  <done>8 structural fixtures are committed with a passing digest-oracle round-trip.</done>
</task>

<task type="auto">
  <name>Task 2: Type/nullability fixture category (6 fixtures)</name>
  <files>tests/fixtures/corpus.yaml, tests/fixtures/CORPUS.sha256</files>
  <read_first>
    tests/fixtures/corpus.yaml, configs/datasets/orders.json (for a real decimal/date column shape
    to model the fixture values against),
    .planning/phases/02-config-contract-csv-generator/02-RESEARCH.md (section "Recommended scoped
    fixture category list", row "Type/nullability")
  </read_first>
  <action>
    Author exactly 6 fixtures into `tests/fixtures/corpus.yaml` under category
    `type_nullability` (this project's own vocabulary — no reference-repo file-for-file match,
    since the reference repo's type-damage fixtures are entangled with its out-of-scope locale/
    normalization profile): `17_invalid_integer_value` (tabular, a non-numeric string in a
    column shaped like an integer column); `18_invalid_decimal_too_many_places` (tabular, an
    `amount`-shaped decimal value with 3+ decimal places — `expect`: "exceeds orders.amount's
    configured scale=2"); `19_invalid_date_format` (tabular, a date value not matching
    `"%Y-%m-%d"`, e.g. `"31/02/2026"`); `20_invalid_timestamp_format` (tabular, a timestamp value
    not matching `"%Y-%m-%dT%H:%M:%S%z"`); `21_empty_required_field` (tabular, a non-nullable
    column's value is an empty string); `22_empty_nullable_field_should_pass` (tabular, a nullable
    column's value is an empty string — `expect`: "SHOULD validate cleanly; documents the
    nullable=true happy path, not an error case" — this is the one fixture in the category that is
    NOT a failure case, make this explicit in its `expect:` text). Run `make fixtures && make
    fixtures-verify`.
  </action>
  <verify>
    <automated>make fixtures && make fixtures-verify</automated>
  </verify>
  <acceptance_criteria>
    - `make fixtures-verify` exits 0.
    - The manifest now declares 22 total fixtures; `wc -l tests/fixtures/CORPUS.sha256` reports 22 lines.
    - `grep -n "22_empty_nullable_field_should_pass" tests/fixtures/corpus.yaml` shows an `expect:` value containing the word "pass" or "clean" (not an error-shaped description).
  </acceptance_criteria>
  <done>6 type/nullability fixtures are committed, including the one explicit should-pass case, with a passing digest-oracle round-trip.</done>
</task>

<task type="auto">
  <name>Task 3: Byte-level-hard fixture category (5 fixtures)</name>
  <files>tests/fixtures/corpus.yaml, tests/fixtures/CORPUS.sha256</files>
  <read_first>
    tests/fixtures/corpus.yaml, tools/corpus/generators.py,
    .planning/phases/02-config-contract-csv-generator/02-RESEARCH.md (section "Recommended scoped
    fixture category list", row "Byte-level-hard")
  </read_first>
  <action>
    Author exactly 5 fixtures into `tests/fixtures/corpus.yaml` under category `byte_level_hard`:
    `23_embedded_newline_in_quoted_field` (tabular, a quoted field value containing a literal
    `\n`); `24_embedded_delimiter_in_quoted_field` (tabular, a quoted field value containing the
    configured delimiter character); `25_doubled_quote_escaping` (tabular, a field using RFC-4180
    `""` doubled-quote escaping); `26_embedded_nul_byte` (literal, a data row containing a raw
    `\x00` byte); `27_oversized_field_value` (literal, a field value exceeding 10,000 characters —
    `expect`: "field value exceeds a documented size threshold"). If any of these require a
    `generators.py` capability the `tabular`/`literal` kinds don't already expose (e.g. raw NUL
    byte injection inside an otherwise-tabular row), fall back to the `literal` kind with the full
    row hand-constructed — do not add a new `GeneratorKind` for this category. Run `make fixtures
    && make fixtures-verify`.
  </action>
  <verify>
    <automated>make fixtures && make fixtures-verify</automated>
  </verify>
  <acceptance_criteria>
    - `make fixtures-verify` exits 0.
    - The manifest now declares 27 total fixtures; `wc -l tests/fixtures/CORPUS.sha256` reports 27 lines.
    - Re-running `make fixtures && make fixtures-verify` a second time still exits 0.
  </acceptance_criteria>
  <done>27 of the corpus's ~30 planned fixtures (everything except large/compressed) are committed with a passing digest-oracle round-trip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary — this plan only authors additional fixture content through Plan 03's
already-mitigated `yaml.safe_load()`-only manifest loader and already-vetted `PyYAML` dependency.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 (inherited) | Tampering | `tests/fixtures/corpus.yaml` (extended) | high | mitigate | Same `yaml.safe_load()`-only mechanism from 02-03-PLAN.md; no new deserialization path is introduced by adding fixture entries |

</threat_model>

## Artifacts this phase produces

- **New fixture entries** in `tests/fixtures/corpus.yaml` (category `structural`):
  `09_missing_column`, `10_extra_unexpected_column`, `11_duplicate_column_name`,
  `12_wrong_column_count_row`, `13_empty_file`, `14_header_only_no_rows`, `15_no_header_row`,
  `16_ragged_rows_and_blank_lines`.
- **New fixture entries** (category `type_nullability`): `17_invalid_integer_value`,
  `18_invalid_decimal_too_many_places`, `19_invalid_date_format`, `20_invalid_timestamp_format`,
  `21_empty_required_field`, `22_empty_nullable_field_should_pass`.
- **New fixture entries** (category `byte_level_hard`): `23_embedded_newline_in_quoted_field`,
  `24_embedded_delimiter_in_quoted_field`, `25_doubled_quote_escaping`, `26_embedded_nul_byte`,
  `27_oversized_field_value`.
- **`tests/fixtures/CORPUS.sha256`** extended from 8 to 27 entries.
- No new production symbols (classes/functions/CLI flags) — this plan authors manifest content
  only, against Plan 03's already-built `tools/corpus` mechanism.

<verification>
- `make fixtures && make fixtures-verify` exits 0 after each task, with the fixture count growing 8 -> 16 -> 22 -> 27.
</verification>

<success_criteria>
- 27 fixtures across dialect/encoding, structural, type/nullability, and byte-level-hard categories are committed with a passing digest-oracle round-trip.
</success_criteria>

<output>
Create `.planning/phases/02-config-contract-csv-generator/02-04-SUMMARY.md` when done
</output>
