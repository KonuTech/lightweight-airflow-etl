---
phase: 02-config-contract-csv-generator
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - configs/datasets/orders.json
  - tests/unit/test_config_loader.py
  - tests/unit/test_generate_csv.py
  - tests/unit/test_config_models.py
autonomous: true
requirements: [CONFIG-01, CONFIG-02, GEN-01]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Loading configs/datasets/orders.json via load_config() returns a validated DatasetConfig whose oracle.valid_table/invalid_table equal 'orders_valid'/'orders_invalid' and whose columns exactly match docker/oracle/init/03_orders.sql's non-engine-managed columns."
    - "generate_csv.py --dataset orders produces a CSV whose valid rows' `amount` values never contain more than 2 decimal places (config precision=12, scale=2), and whose invalid rows can exercise all four D-15 categories (orders has both a decimal column for wrong_type and a date column for invalid_date)."
    - "test_config_models.py proves: an unrecognized top-level or nested key in a dataset config raises pydantic.ValidationError (extra=\"forbid\" on every model class); a decimal column declaring scale > precision raises ValidationError; a date/timestamp column with no `format` raises ValidationError; csv.delimiter equal to csv.decimal_separator raises ValidationError; each of escapechar/doublequote/lineterminator/decimal_separator round-trips through CsvDialectConfig with its declared non-default value (D-02's explicit test-coverage request)."
    - "DatasetConfig.columns rejects an empty list (min_length=1) for the orders schema too, not just customers — the constraint is on the shared model, not a per-dataset check."
  prohibitions: []
  artifacts:
    - configs/datasets/orders.json
    - tests/unit/test_config_models.py
  key_links:
    - "load_config(configs/datasets/orders.json, defaults_path=configs/defaults.json) -> DatasetConfig -> generator/generate_csv.py --dataset orders (same code path Plan 01 proved for customers)"
---

<objective>
Extend Plan 01's proven config-contract + generator pipeline to the second dataset (`orders`,
completing CONFIG-01/GEN-01's "both datasets" requirement) and add the comprehensive Pydantic
validation-rule test suite CONTEXT.md's D-17 assigns to `test_config_models.py` (as distinct from
Plan 01's `test_config_loader.py`, which covers `load_config()`'s success/failure/merge behavior).
No new production code path is introduced — `generator/generate_csv.py` and
`csv_processor.config.models`/`loader` are already dataset-agnostic from Plan 01; this plan proves
it and locks down every Pydantic validation rule with a real test.

Purpose: close out CONFIG-01/CONFIG-02/GEN-01's full requirement text ("both `customers` and
`orders`") and D-17's test-layout decision before the phase moves to the independent
fixture-corpus subsystem (Plans 03-05).
Output: `configs/datasets/orders.json`, a comprehensive `tests/unit/test_config_models.py`, and
extended assertions in `test_config_loader.py`/`test_generate_csv.py` proving the `orders` path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-config-contract-csv-generator/02-CONTEXT.md
@.planning/phases/02-config-contract-csv-generator/02-RESEARCH.md
.planning/phases/02-config-contract-csv-generator/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `orders.json` dataset config + prove the generator/loader path for `orders`</name>
  <files>configs/datasets/orders.json, tests/unit/test_config_loader.py, tests/unit/test_generate_csv.py</files>
  <read_first>
    docker/oracle/init/03_orders.sql,
    configs/datasets/customers.json (Plan 01's output — the shape to mirror),
    packages/csv-processor/src/csv_processor/config/models.py,
    generator/generate_csv.py,
    tests/unit/test_config_loader.py,
    tests/unit/test_generate_csv.py
  </read_first>
  <behavior>
    - Test: `load_config(Path("configs/datasets/orders.json"), defaults_path=Path("configs/defaults.json"))` returns `dataset == "orders"`, 4 columns, `oracle.valid_table == "orders_valid"`, `oracle.invalid_table == "orders_invalid"`, and the `amount` column has `type == "decimal"`, `precision == 12`, `scale == 2`.
    - Test: `generate_csv.py --dataset orders --rows 50 --invalid-ratio 0.2 --seed 42` run twice produces byte-identical output (same determinism proof as Plan 01, for the second dataset).
    - Test: at least one generated invalid row for `orders` uses the `wrong_type` category (targeting `amount`) and at least one uses `invalid_date` (targeting `order_date`) across a sample large enough to make both likely (e.g. `--rows 200 --invalid-ratio 0.5 --seed 42`) — proving `orders`' richer schema (it has a decimal AND a date column) exercises categories `customers` alone cannot.
    - Test: every valid `amount` value in the generated CSV, parsed as `Decimal`, has an exponent of exactly `-2` (i.e. exactly 2 decimal places, never more, never fewer).
  </behavior>
  <action>
    Create `configs/datasets/orders.json` (D-04) with `dataset: "orders"`, `file_pattern:
    "orders_*.csv"` (D-07), and `columns` matching `docker/oracle/init/03_orders.sql:11-17` exactly
    (excluding the engine-managed `ingested_at` column): `order_id` (string, nullable=false,
    required=true), `customer_id` (string, nullable=false, required=true — FK to
    `customers.customer_id`, unenforced per spec §28, no referential check added), `order_date`
    (date, nullable=true, required=true, format `"%Y-%m-%d"`), `amount` (decimal, nullable=true,
    required=true, precision=12, scale=2 — matches `NUMBER(12,2)` exactly per D-10); `oracle:
    {"valid_table": "orders_valid", "invalid_table": "orders_invalid"}`; `processing:
    {"chunk_size": 5000}`.

    Extend `tests/unit/test_config_loader.py` with an `orders`-specific load-success test mirroring
    the existing `customers` one. Extend `tests/unit/test_generate_csv.py` with the `orders`-specific
    determinism/category/decimal-formatting tests in `<behavior>` above. Do not modify
    `generator/generate_csv.py` or `csv_processor.config.{models,loader}` — both are already
    dataset-agnostic from Plan 01; this task only proves the second dataset works through the
    existing code paths.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_config_loader.py tests/unit/test_generate_csv.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_config_loader.py tests/unit/test_generate_csv.py -x` passes with zero failures.
    - `uv run python generator/generate_csv.py --dataset orders --rows 20 --invalid-ratio 0.25 --seed 7` exits 0 and creates `./data/orders/orders_<today>.csv` with header `order_id,customer_id,order_date,amount`.
  </acceptance_criteria>
  <done>Both `customers` and `orders` validate and generate through the identical, unmodified config/generator code path — CONFIG-01/GEN-01's "both datasets" requirement is fully met.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `test_config_models.py` — comprehensive Pydantic validation-rule suite (D-17)</name>
  <files>tests/unit/test_config_models.py</files>
  <read_first>
    packages/csv-processor/src/csv_processor/config/models.py,
    /home/user/projects/airflow-platform/tests/unit/test_dataset_config_columns.py (lines 100-124 —
    the reference repo's own delimiter/decimal-separator collision test shape, precedent for this
    task's equivalent test)
  </read_first>
  <behavior>
    - Test: a fully-specified valid `DatasetConfig` dict (matching `customers.json`'s shape)
      round-trips through `DatasetConfig.model_validate()` without error.
    - Test: each of the six `ColumnSpec.type` literal values is accepted; an unrecognized type
      string (e.g. `"varchar"`) raises `ValidationError`.
    - Test: `ColumnSpec(type="decimal", precision=12, scale=2, ...)` validates; `scale > precision`
      (e.g. `precision=2, scale=5`) raises `ValidationError`; omitting `precision` or `scale`
      entirely on a `decimal` column raises `ValidationError`.
    - Test: `ColumnSpec(type="date", format=None, ...)` and `type="timestamp", format=None` both
      raise `ValidationError`; the same types WITH a `format` string validate.
    - Test: `nullable`/`required` accept every one of the four boolean combinations independently
      (D-09 — they are never collapsed into one flag).
    - Test: `CsvDialectConfig(escapechar="\\", doublequote=False)` round-trips with both non-default
      values readable back off the instance; same for `lineterminator="\r\n"` and
      `decimal_separator=","` (each field independently, per D-02's explicit test-coverage request).
    - Test: `DatasetConfig` with `csv.delimiter == csv.decimal_separator` (e.g. both `","`, or both
      `";"`) raises `ValidationError` from `_check_delimiter_does_not_collide_with_decimal_separator`;
      `DatasetConfig` with `csv.delimiter=";"` and `csv.decimal_separator=","` (different characters)
      validates cleanly.
    - Test: an unrecognized key at the top level of the merged dict, and one nested inside `csv`/
      `oracle`/`processing`, each raise `ValidationError` (`extra="forbid"` on every model class).
    - Test: `DatasetConfig(columns=[])` raises `ValidationError` (`Field(min_length=1)`).
    - Test: constructing a valid `DatasetConfig` then attempting `config.dataset = "other"` raises
      `ValidationError` (frozen=True).
    - Test (privacy prohibition, T-02-02 backing evidence): iterate every field name declared across
      `ColumnSpec`, `CsvDialectConfig`, `OracleTargetSpec`, `ProcessingConfig`, `DatasetConfig` via
      each model's `model_fields` and assert none of them, case-insensitively, contains any of
      `"password"`, `"secret"`, `"credential"`, `"connection"`, `"conn_str"`, `"dsn"` — a mechanical
      proof that the schema structurally cannot carry a credential field (backs
      `must_haves.prohibitions` in 02-01-PLAN.md).
  </behavior>
  <action>
    Create `tests/unit/test_config_models.py` (D-17) as either stdlib `unittest.TestCase` classes
    (matching `tests/test_verify_environment.py`'s existing convention) or plain `pytest` functions
    (Claude's discretion, per CONTEXT.md), covering every case listed in `<behavior>` above. Group
    tests into logical classes/sections: type validation, precision/scale, nullable/required
    combinations, CSV dialect extra-field round-trips, delimiter/decimal-separator collision,
    `extra="forbid"` enforcement, frozen-instance enforcement, and the credential-field-name
    mechanical scan.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_config_models.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_config_models.py -x` passes with zero failures.
    - `uv run pytest tests/unit/test_config_models.py -v 2>&1 | grep -c "PASSED\|test_"` reports at least 10 distinct test cases (one per `<behavior>` bullet, several bullets covering more than one assertion).
  </acceptance_criteria>
  <done>Every Pydantic validation rule in `csv_processor.config.models` — types, precision/scale, nullable/required, D-02's extra dialect fields, the delimiter/decimal-separator collision, extra="forbid", frozen — has a passing, explicit unit test; the credential-field-name scan mechanically backs the phase's privacy prohibition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary is introduced by this plan — `orders.json` is the same class of
developer-authored, version-controlled input as `customers.json` (Plan 01), and
`test_config_models.py`/extended test files are test code, not a runtime attack surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 (inherited) | Tampering | `configs/datasets/orders.json` | medium | mitigate | Same `extra="forbid"` mechanism as `customers.json` (see 02-01-PLAN.md); this plan's Task 2 adds the explicit unit test proving it |
| T-02-02 (inherited, now mechanically proven) | Information Disclosure | `csv_processor.config.models` (all classes) | high | mitigate | Task 2's credential-field-name scan is the concrete test backing 02-01-PLAN.md's `must_haves.prohibitions` entry — no field name resembling a credential exists anywhere in the model tree |

</threat_model>

## Artifacts this phase produces

- **New file**: `configs/datasets/orders.json` (dataset config for `orders`, mirrors
  `customers.json`'s shape).
- **New test module**: `tests/unit/test_config_models.py` (no new production symbols — a
  comprehensive test suite over Plan 01's existing `csv_processor.config.models` classes).
- **New test functions** added to the existing `tests/unit/test_config_loader.py` and
  `tests/unit/test_generate_csv.py` (orders-specific load/generate/determinism assertions) — no
  new production code paths, `generator/generate_csv.py` and `csv_processor.config.{models,loader}`
  are unmodified by this plan.

<verification>
- `uv run pytest tests/unit/ -x` — full unit suite (loader, generator, models) passes for both datasets.
</verification>

<success_criteria>
- `customers` and `orders` both validate and generate through the identical config/generator code path.
- Every Pydantic validation rule declared in Plan 01's `models.py` has an explicit, passing unit test per D-17.
</success_criteria>

<output>
Create `.planning/phases/02-config-contract-csv-generator/02-02-SUMMARY.md` when done
</output>
