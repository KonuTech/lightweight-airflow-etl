.env
# Phase 8 gap-closure: the passwords file now lives inside the git-tracked
# docker/airflow/secrets/ directory (see .gitkeep there) instead of being bind-
# mounted directly by itself -- ignore only the generated file, not the directory.
docker/airflow/secrets/simple_auth_manager_passwords.json.generated
.venv/
__pycache__/
*.pyc

# D-06: generate_csv.py's output -- generated content, never committed.
/data/
# D-16e: tools/corpus's generated fixture bytes -- the manifest + digest
# oracle (corpus.yaml / CORPUS.sha256) ARE committed; only the bytes aren't.
/tests/fixtures/csv/
